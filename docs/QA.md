# 최종 검증 기록

검증일: 2026-09-22 · 코드 기준: `76849bc` 이후 제출 문서 정리본. 이번 문서 정리는 앱의 추천 수식·데이터·UI를 변경하지 않았습니다. 이전 상세 검증 기록은 Git 이력에 보존됩니다.

## 자동 검사와 데이터 분석

| 검사 | 결과·범위 |
|---|---|
| 자동 테스트 | 15개 파일 200개 통과. 실제 검사 항목은 아래 참조 |
| 타입·프로덕션 빌드 | TypeScript 및 Vite 통과 |
| 원본 보존 | 외부 원본과 앱 CSV 동일 SHA-256, 200명 |
| 데이터 감사 | 비용 미확인·평점 공란 27명, 평균×건수 불일치 173명 |
| A/B/C 대안 | 150조건, 76개 비어 있지 않음, 조건 위반 0; A–C 1위 차이45, B–C14 |
| 가중치 민감도 | 균등 ±20% 상대 변경 608회 중 1위 변경35; 4프리셋 추가 진단은 47조건·각376회 |
| Mermaid | 로컬 Mermaid 11.17.2 파서로 3개 문법 검사 통과. 공개 GitHub 렌더링과 구분 |
| 문서 연결·공개 범위 | 아래 최종 패키지 확인 기록 참조 |

## 시험 범위

| 파일 | 확인한 내용 |
|---|---|
| domain.test.ts | 원본 해시·스키마·중복·경계 예산·규모·OR·0/1/다수·견적 분리·점수 합·동점·비교 집단·대안 수 |
| weights.test.ts | 4프리셋×4지표×0–100 조절, 합계·유효성·분배 |
| setup.test.ts | 설명 추출·필수 누락·총예산 오해 방지·수동 이름/비중 보존·목표 변경 |
| experience.test.ts | 저장 복원·공란·비교 메모·503 후 재요청·잘못된 응답·비교 집단 설명 |
| matchStory.test.ts | 우선 지표·비중과 카피 일치, 근거 없는 강점 방지 |
| campaign.test.ts, collaborationFlow.test.ts, sentHistory.test.ts | 문안·협업·성과 입력·링크·빈값/0·분모·발송 기록과 실제 연락 구분 |
| campaignFit.test.ts, campaignInsights.test.ts, review.test.ts | 캠페인별 설명·원본/순위 불변·근거 ID·문의/리포트 일치·기존 문안 보존 |
| contentValidation.test.ts, discovery.test.ts, searchPolicy.test.ts | 자료 등록·댓글·원천 구분·키워드·자료 부족·제외 판정 |
| assistant.test.ts | 요청·응답 구조. 실제 모델 품질 시험 아님 |

## 데스크톱에서 관찰한 핵심 경로

1. 예시 설명을 입력해 제품·타깃·200만 원·뷰티/패션·마이크로가 다음 단계에 전달됨. 세 단계 모두 왼쪽 안내·오른쪽 입력, 나노/마이크로/매크로와 숫자 구간 표시.
2. 추천 완료 후 21명·비용 미확인 3명. 조회수 우선에서 정은매거진77, 참여율 우선 적용 후 서연리뷰66. 원조회수 정렬도 별도로 작동.
3. 상세에 현재 비중과 실제 비교 집단을 표시. 평점 공란 후보는 미평가·견적 문의로 표시.
4. 예산 1원 → 적격 0명·미확인 3명 → 540,000원 제안과 추가액 확인 → 적용 후 소라TV177 한 명.
5. 후보별 적합 문단 → 해당 콘텐츠·광고 댓글 근거 → 선택 질문만 문의로 전달. 기존 문안·진행 상태 유지.
6. 문의 문안·조건 기록, 같은 후보의 서로 다른 캠페인 문안·상태 전환 및 새로고침 복원.
7. 한 캠페인에서 조회수12,000·집행비780,000·구매0·확인일·출처·집계 기준 입력 후 저장. 입력 창이 닫히고 성과 리포트 갱신.
8. 분석 리포트 복사 내용과 화면 문단 일치. 파일 다운로드 완료는 확인하지 못함.

이 목록은 각 개발 단계에서 실제 관찰한 경로입니다. 모든 선택 기능의 모든 조합을 최종 상태에서 다시 수행한 전체 회귀 시험으로 확대 해석하지 않습니다. 최신 온보딩·추천 핵심 경로는 `743376b`, `76849bc`에서 재확인했습니다.

## 아직 확인하지 않은 것

- 공개 GitHub의 비로그인 접근·Mermaid 렌더링·원격 커밋·외부 링크 상태.
- 실제 모델 응답 성공·의미 정확성·비용·지연. 로컬 규칙과 응답 형식 시험은 이 검증을 대체하지 못함.
- 서로 다른 캠페인 성과를 브라우저에서 연속 입력·전환하는 전체 경로. 단일 캠페인 저장과 캠페인별 문안·상태는 관찰했지만 같은 범위로 주장하지 않음.
- 다운로드 파일의 저장 완료, 실제 한글 IME 조합 전체, 모든 검색·완화 조건 조합.
- 새 OS·캐시 없는 네트워크 설치, 실제 고객 사용성·광고 성과. 모바일은 범위 제외.

## 실행상 관찰

테스트에서 Node의 `--localstorage-file` 경고가 출력되지만 검사는 통과했습니다. 온보딩 전환의 4.2초는 시각적 안내이며 계산 성능 측정값이 아닙니다. 성과·추천 정확도·시간 절감의 개선율은 측정하지 않았습니다.

## 최종 패키지 확인

- 현재 제출 파일을 별도 빈 폴더에 복사하고 `npm ci --offline --no-audit --no-fund`로 100개 패키지를 설치했습니다. 같은 컴퓨터·기존 npm 캐시이며 새 OS·새 네트워크 검증은 아닙니다.
- 해당 폴더에서 15개 파일 200개 테스트, 타입 검사, 프로덕션 빌드를 통과했습니다. 원본 프로젝트의 설치 폴더를 복사하지 않았습니다.
- 24개 Markdown의 로컬 링크를 확인해 누락 0건, Mermaid 3개 문법 통과. 외부 사이트 전체 링크의 현재 접근성을 다시 검사한 것은 아닙니다.
- 기존 32개 커밋의 336개 파일 버전을 검사했습니다. 과제 PDF·비밀 환경 파일·알려진 개인 이메일·API 키/개인 키 패턴은 발견되지 않았습니다. 모든 종류의 비밀을 탐지한다는 보장은 아닙니다.
- 외부 원본과 앱 CSV가 바이트 단위로 동일하며 SHA-256은 DATA_AUDIT와 같습니다.
- 제출 이메일과 개인 검토 체크리스트는 저장소 밖의 ‘제출 검토’ 폴더에 두었습니다. 원격 등록·메일 발송은 하지 않았습니다.
