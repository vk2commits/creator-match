# Creator Match · PRD

상태: 브랜드 실무 마케터의 업무 흐름을 반영한 다섯 번째 구현. 최종 사용자 리뷰 전. 실제 검증 결과는 [QA](docs/QA.md)에서 추적. 2026-09-21.

## 문제와 사용자

1차 사용자는 사용자가 선택한 **브랜드 실무 마케터**다. 크리에이터 협업 경험이 있고, 캠페인 분야·예산을 받아 견적 문의 대상과 선정 이유를 팀에 제안하는 상황을 가정한다. 후보 제안은 담당자가 하지만 계약·최종 예산 승인은 팀 검토가 필요하다는 권한 가설을 둔다. 나이·성별 같은 무관한 인구통계를 만들지 않는다.

핵심 업무는 **문의할 대상과 이유가 담긴 팀 검토안**이다. 5차 개선은 그다음 문의 준비·협업 상태·집행 결과 기록으로 이어지게 한다. 후보 보관은 중간 단계이며 한 명만 있어도 완료할 수 있다. 실제 인터뷰나 행동 관찰은 수행하지 않았다. [가설 페르소나·과업 검증](docs/PERSONA_TEST.md)에 선택 근거, 이전 흐름의 문제, AI의 실제 기능 점검과 인간 사용자 테스트 계획을 구분한다.

공개 서비스의 탐색·분석·업무 연결 기능과 IAB의 측정 한계는 [리서치](docs/RESEARCH.md)에 기록했다. 이 자료는 문제의 맥락을 뒷받침하며 이 프로토타입의 수요나 성과를 입증하지 않는다.

## 목표와 범위

- 목표: 브리프 → 조건 → 추천 기준·직접 비중 → 탐색·비교 → 문의 준비 → 협업 상태 → 성과 기록. 실제 발송·콘텐츠 확인은 외부에서 하고 확인한 내용만 기록한다.
- 성공 기준: 조건 위반 0, 미확인 가격의 무료 처리 0, 설명과 계산 일치, 빈 결과에서 다음 행동 가능.
- 비목표: 실제 ROAS 예측, 계약 자동 체결, 여러 명의 총예산 조합 최적화, 자동 연락·자동 수집·결제·로그인·서버 DB. 단일 작업실의 수동 기록과 선택적 로컬 AI 연결은 사용자 요청으로 추가한다.
- 선택 가산 기능: 추천 근거 설명, 다섯 가지 정렬. 사용자 피드백 이후 추가: 첫 설정, 추천 기준 전환, 보관과 비교 선택 분리, 최대 3명 동시 비교, 직접 비중 조절, 문의/보류 상태, 직접 작성하는 팀 검토안.
- 사용자 검증 전 가설: 설명을 보면 후보 검토에 대한 납득이 높아진다. 아직 시간 절감·만족도·전환율을 측정하지 않았다.

## 입력 정책

| 항목 | 정책 | 수용 기준 |
|---|---|---|
| 예산 | 1명·1회 캠페인 상한, 원화 양의 안전한 정수 | 빈 값·0·음수·소수·지수·문자 거절; 쉼표 표시·붙여넣기 허용; 동일 예산 포함 |
| 카테고리 | 10개 중 하나 이상, 다중 선택 OR | 선택 분야 중 하나만 같아도 통과 |
| 규모 | 한 구간 선택 | 나노 <10,000; 마이크로 10,000 이상 100,000 미만; 매크로 100,000 이상 |
| 적용 | 첫 설정 또는 조건 변경 창의 적용 버튼 | 취소·오류 시 기존 조건과 결과 유지 |

규모는 본 프로토타입의 운영 정의다. 단일 업계 표준으로 주장하지 않는다. 예산은 입력하면서 천 단위 쉼표를 표시한다. 입력 폼의 규모 이름은 팔로워 수를 먼저, 나노·마이크로·매크로는 보조로 배치한다.

## 데이터와 적격 판정

원본 200명·11필드. 파일 해시는 [데이터 감사](docs/DATA_AUDIT.md)에 기록한다. 원본 복사본을 그대로 읽고 별도 메모리 객체만 변환한다. CSV 구조·중복 ID·범주·숫자 범위를 먼저 검증하고 오류가 있으면 일부 데이터로 조용히 진행하지 않는다.

1. 카테고리 OR 및 규모 필터.
2. 캠페인 건수 >0이고 제공 평균 예산 >0이면 가격 참고값이 있는 후보.
3. 해당 후보의 평균 예산 ≤ 입력 예산이면 적격.
4. 가격 참고값이 있으나 입력 예산을 초과한 후보는 추천에서 제외하고 초과 인원에 집계한다.
5. 이력 또는 양수 평균 비용이 없는 후보만 동일 분야·규모의 ‘비용 미확인’으로 분리한다. 이들은 예산 적합을 보장하지 않으며 점수를 표시하지 않는다.

이력이 없는 27명의 0원은 무료가 아니다. 평점 공란은 미평가다. 과거 평균 예산은 현재 견적이 아니며 제작 범위·사용권 등에 따라 달라진다. 유이력 173명의 평균×건수와 누적액이 모두 불일치하므로 누적액으로 제공 평균을 덮어쓰지 않는다. 누적액은 점수에 쓰지 않는다.

참여율 7.7은 7.7%다. 조회수는 제공 평균 콘텐츠 조회수이며 광고 예상 조회수로 표현하지 않는다. 측정 기간·참여율 분모·평점 응답 수는 제공되지 않았다.

## 추천 수식

정책의 기준 파일은 `src/policy.ts`, 순수 계산은 `src/domain.ts`다.

첫 방문에 기본 추천 기준을 강제로 선택하지 않는다. 사용자는 참여율·조회수·협업 이력의 세 선택지 중 하나를 고르며, ‘아직 정하지 않았어요’를 선택하면 균등 비교를 사용한다. 예시 캠페인은 균등 비교로 시작하며 결과 상단에 적용 기준을 표시한다. 첫 단계에서도 선택적으로 비중을 직접 조절할 수 있다. 기존 35/35/20/10을 고정 기본값으로 보여주는 안은 지원자의 실제 UX 피드백으로 기각했다.

| 기준 | 참여율 | 조회수 | 평점 | 경험 |
|---|---:|---:|---:|---:|
| 참여율 우선 | 50% | 25% | 15% | 10% |
| 조회수 우선 | 20% | 55% | 15% | 10% |
| 협업 이력 우선 | 20% | 20% | 30% | 30% |
| 균등 비교 | 25% | 25% | 25% | 25% |

총점은 각 지표의 0~100 환산값에 선택한 가중치를 곱한 합이다. 가중치는 프리셋의 의미를 표현한 제품 가정이며 학습한 최적값이나 성과 확률이 아니다. 균등도 중립적인 정답이 아니다. 결과 상단에서 기준을 전환하면 동일 적격 집합의 순서만 달라지고 정렬은 추천순으로 돌아간다.

팔로워·예산·카테고리는 자격 조건이며 다시 가산하지 않는다. 팔로워·누적 예산 추가 가산은 규모의 중복 반영을 피하기 위해 제외했다. 새로운 프리셋 검증은 [우선순위 비교](docs/PRIORITY_EVALUATION.md)에 기록한다. 프리셋만 제공하는 것이 더 쉽다는 실제 사용자 근거는 없었다. 숙련 마케터의 통제권을 반영해 프리셋과 직접 조절을 함께 제공한다. 프리셋 값 자체와 자동 조절의 편의성은 별개 가설이다.

### 직접 비중 조절

- 네 지표 각각 0~100%, 정수 단위. 슬라이더 드래그·화살표/Home/End·숫자 입력을 제공한다.
- 지표 하나를 x%로 바꾸면 나머지 100−x%는 기존 세 지표의 상대 비율로 재분배한다. 다른 세 값이 모두 0이면 균등 분배한다. 한 번의 드래그·키보드 입력 중에는 조작을 시작한 비율을 유지해 반복 반올림에 따른 쏠림을 막는다.
- 정수화는 내림 후 소수 부분이 큰 순서로 잔여 1%를 배분한다. 동률은 참여율→조회수→평점→경험 순으로 고정한다. 합계는 항상 100%다. `src/weights.ts`가 단일 정책이다.
- 네 값과 재분배 안내를 함께 보여준다. 임의의 값 고정 기능은 이번 범위에서 제외한다.
- 결과 화면의 조절 창은 현재 조건을 유지한 상위 3명 미리보기를 보여준다. 적용 전까지 기존 결과는 유지하며 취소·닫기·Escape는 임시 변경을 버린다. 첫 설정은 조건 입력 이전이므로 순위 미리보기를 하지 않는다.
- 적용하면 목록·상세·비교·팀 검토안이 같은 사용자 비중을 쓴다. 조건 변경·대안 적용·새로고침은 직접 비중을 보존한다. 프리셋 버튼을 고르면 해당 프리셋으로 돌아간다.
- 0% 지표는 점수와 설명의 주요 근거에서 제외한다. 참여·조회가 모두 0%이면 평점·경험 중 실제 반영된 지표를 설명한다.
- 100% 선택도 허용한다. 특정 지표를 최대화하는 취향은 표현할 수 있지만 추천 정확도나 성과 최적성은 보장하지 않는다. 조회수 100%도 플랫폼·규모 내 상대 위치이므로 전체 원시 조회수 정렬과 다를 수 있다.

### 참여율·조회수

고정된 원본 전체에서 동일 플랫폼×규모 집단을 사용한다. 집단이 10명 미만이면 동일 플랫폼 전체, 그것도 10명 미만이면 전체 데이터로 확대한다. 비교 집단 이름·표본 수·확대 여부를 설명한다. 현재 집단은 13~69명이다.

값보다 작은 수를 L, 같은 수를 E, 집단 크기를 N이라 하면:
`상대 위치 = 100 × (L + (E−1)/2) / (N−1)`. N<2이면 50.
동점은 중간 위치다. 극단값의 크기가 아니라 순서를 사용해 이상치 영향을 줄이나 절대 차이를 잃는 단점이 있다. “상위 몇 %”라는 확률적 표현 대신 “집단 내 상대 위치”로 표시한다. 사용자 필터가 바뀌어도 같은 후보 점수는 유지된다.

### 평점·경험

평점은 `(평점−1)/4×100`. 공란은 계산에만 척도 중간의 대체값 50을 사용하고 화면에는 ‘미평가’로 표기한다. 현재 공란 27명은 모두 견적 미확인 영역에 있어 종합 점수를 노출하지 않는다. 과거 이력이 있으나 평점이 없는 가상 테스트로 대체값 정책도 검증한다. 50은 평점 척도 3.0에 해당하며 현재 관측값 3.4~5.0보다 낮다. 불이익 없는 중립값이나 통계 추정값이 아니다. 향후 실제 결측이 적격 후보에 생기면 재분배·결측 분리안과 비교 검증해야 한다.

경험은 `log(1+건수)/log(1+전체 최대 건수)×100`. 최대 건수의 분모는 최소 1로 보호한다. 경험 증가의 가산을 체감시킨다. 캠페인 건수는 평점 응답 수가 아니므로 베이지안 평점 보정에 사용하지 않는다.

### 정렬·설명

추천순·참여율·평균 조회수·집행 건수·평점 내림차순. 평점 미평가는 마지막. 동점은 creator_id 오름차순. 계산·정렬은 반올림 전 값, 상세의 총점·기여도는 소수 둘째 자리이므로 표시 합에 반올림 오차가 있을 수 있다.

목록은 참여율·조회수 중 `(상대 위치−50)×선택 가중치`가 더 큰 지표를 요약한다. 양수 가중치인 지표만 우선 비교한다. 이는 총점의 유일한 원인이 아니라 관측된 상대 강점이다. 중간값보다 낮다면 낮다고 표시하며 긍정적인 말로 포장하지 않는다. 조회수는 같은 비교 집단 중간값의 약 몇 배인지, 참여율은 몇 %p 차이인지 표시한다. 소수점 한 자리·‘약’ 표시는 읽기 위한 반올림이다. 상세에는 실제 값·중간값·비교 집단·인원을 명시한다.

비교표는 선택한 다른 적격 후보 중 추천 점수가 가장 높은 1명을 기준으로 각 지표의 **가중 기여도 차이**를 계산한다. 가장 큰 양의 차이는 상대 강점, 가장 큰 음의 차이는 상대 후보가 유리한 지표로 설명한다. 양의 차이가 없거나 동점이면 그대로 알리고, 다른 적격 후보가 없으면 상대를 만들지 않는다. 플랫폼이 다르면 원시 조회수 차이가 아닌 각 집단 안에서의 위치를 비교한다. 조건 밖·비용 미확인 후보는 이 순위 비교에서 제외한다. 여러 지표의 종합 우열이나 브랜드 적합성을 확정하는 설명이 아니다.

점수 함수가 비교 집단과 참여·조회 중간값을 함께 반환하고 목록·상세·메모가 이를 공유한다. 표본 부족으로 집단을 확대해도 UI가 중간값을 따로 계산하지 않는다. 원본 예산 산술 설명도 후보별로 분기한다. 신규 후보의 0=0에는 불일치 문구를 사용하지 않는다. 목록에는 점수·원본 ID·환산식을 노출하지 않는다. 견적 미확인 후보는 점수 없이 평균 조회수 내림차순, 동률 ID순이다.

## 화면과 예외 정책

한국어 단일 페이지. 결과 상단 **캠페인 설정** 또는 `/#setup`으로 온보딩에 재진입한다. 기존 조건을 초깃값으로 쓰고 취소하면 원래 조건·보관·판단을 보존한다. 서비스 가정은 가입 완료 후 첫 캠페인 설정이며, 데모에는 실제 인증을 넣지 않는다. 첫 방문은 캠페인 브리프 → 분야·예산·팔로워 조건 → 추천 기준·기본 공개된 직접 비중 → 후보 탐색. 예시 우회는 뷰티·패션 / 마이크로 / 2,000,000원 / 균등 비교다.

데스크톱은 정렬된 후보 목록이다. 플랫폼 아이콘에는 접근 가능한 이름을 제공하고 분야는 태그로 표시한다. 결과는 12명씩 더 보고 전체 수를 표시한다. 가격 정보 없는 후보는 ‘비용 미확인’ 탭에서 즉시 확인한다. 기존 반응형 스타일은 남아 있으나, 2026-09-21 지원자의 범위 조정에 따라 모바일 추가 작업·검증은 진행하지 않는다.

후보 보관에는 3명 제한을 두지 않는다. 담은 후보 목록에서 동시 비교할 2~3명을 따로 선택한다. 네 번째 비교 선택은 기존 세 명을 보존하며 해제 후 교체하도록 안내한다. 후보를 보관 목록에서 빼면 비교 선택에서도 빠진다. 보관 순서대로 표시하며 조건을 바꾸어도 목록·비교 선택·작성한 메모를 유지하고 현재 자격을 다시 표시한다. 한 번에 보여줄 비교 열 수와 보관 가치는 다른 문제이므로 유료 제한으로 연결하지 않는다.

후보 상세와 비교표에서 **검토 중 / 문의 후보 / 보류**를 구분한다. 상세에서 상태를 바꾸면 보관 목록에 추가된다. 문의 후보는 상세의 **선정 이유 정리** 또는 하단의 **팀 검토안**으로 진행한다. 두 명 비교가 필수 단계가 아니므로 한 명으로도 완료할 수 있다.

후보별 선정·보류 이유는 500자까지 직접 적는다. 팀 검토안은 전체 보관 목록의 문의 후보와 작성 이유를 포함하고, 보류는 별도로 요약한다. 현재 비교 선택 3명에 한정하지 않는다. 문의 후보 0명이면 돌아갈 행동을 안내한다. 문의 후보의 이유가 하나라도 비어 있으면 복사를 비활성화하고 미작성 인원을 알려준다. 자동 복사가 실패하면 텍스트를 선택해 수동 복사를 안내한다.

팀 검토안에는 문의 대상·직접 쓴 이유·현재 조건·실제 적용 비중·핵심 지표·미확인 상태·다음 행동을 포함한다. 원본 ID·누적액·산술 불일치 같은 상세 데이터는 검토안에 반복하지 않고 상세의 펼침에 둔다. 파일 다운로드 경로는 제거하고 복사로 단일화했다. 실제 메시지나 문의를 발송하지 않는다.

상세의 공통 체크리스트는 제거했다. 후보별로 실제 결측·예산 초과·조건 밖 상태가 있을 때만 안내한다. 현재 견적·사용권·콘텐츠 확인은 실제 문의 준비 필드에 연결한다. 비용 미확인 후보도 견적을 알아보기 위한 문의 대상으로 지정할 수 있지만 예산 충족·추천 순위를 만들어내지 않는다.

이 브라우저의 localStorage에 조건·직접 비중·보관 ID·비교 ID·판단 상태·메모를 저장한다. 저장 형식 v2/v3를 v4로 이전하며 기존 후보를 잃지 않는다. 조건이 바뀌어도 작성한 판단은 보존하되 현재 자격을 다시 표시한다. 보관에서 빼면 비교 선택과 판단 상태에서도 빠진다. 손상된 조건·비중은 첫 설정으로 복구하고 저장이 제한되면 안내한다.

결과 상단 프리셋은 즉시 적용한다. **비중 조절** 창은 적용 버튼 전까지 임시 상태다. 구조화 브라우저 입력은 프리셋 조건을 명시적으로 적용하며, 결과 읽기는 현재의 직접 비중도 반영한다.

전체 11필드의 화면 배치는 [UX 검토](docs/UX_REVIEW.md)에 연결했다. 누적 집행액은 상세, 원본 ID는 추가 펼침, 채널 URL은 미제공으로 명시한다. 더미 인물의 SNS 주소·프로필 사진을 만들어 넣지 않는다.

| 상태 | 사용자에게 보여줄 것 | 다음 행동 |
|---|---|---|
| 로딩 | 데이터 불러오는 중 | 기다리기 |
| 로딩·CSV 오류 | 읽기 실패와 원인 안내 | 다시 불러오기 |
| 입력 오류 | 입력 창에 필드별 이유, 기존 결과 조건 유지 | 해당 입력 수정 |
| 분야·규모 후보 0 | 조합에 후보 없음 | 조건 변경 또는 가능한 규모 제안 적용 |
| 참고 예산 초과 | 예산 때문에 후보가 없는 이유, 같은 조건의 최소 참고 예산과 차액 | 변경값·예상 후보 수를 보고 적용 |
| 견적 미확인만 존재 | 예산 충족을 확인한 후보 없음 | 별도 후보 확인·조건 수정 |
| 적격 후보 있음 | 후보 수, 원지표, 과거 평균 비용, 상세의 추천 이유 | 상세 비교·재정렬 |

대안은 적격 후보가 0명일 때만 생성한다. 같은 분야·규모의 최소 평균 예산, 또는 예산·분야를 유지하는 다른 규모를 제시한다. 분야를 자동 변경하거나 예산 초과 후보를 적격 목록에 끼우지 않는다. 클릭 전에 정확한 변경과 후보 수를 보여준다.

## 검증과 평가

[비교 실험](docs/EVALUATION.md): 동일 적격 후보에 A 팔로워순 / B 전체 상대 위치 가중합 / C 플랫폼×규모 상대 위치 가중합을 적용. 현재 균등 비교로 재실행한 150개 조건 중 결과가 있는 76개에서 A/C 1위 차이 45개, B/C 14개. 조건 위반 0. 608회 가중치 ±20% 상대 변경에서 1위 35회 변경. 네 프리셋을 비교하면 76개 중 34개 조건에서 선택 기준에 따라 1위가 달랐다. 이러한 변화는 정확도 개선 증거가 아니다.

C는 비교 맥락을 설명할 수 있다는 이점 때문에 선택한다. 교차 플랫폼 측정을 완전히 동등하게 만들지는 못한다. 원지표 정렬을 함께 제공하고, 운영 시 카테고리·콘텐츠 형식·기간 차이 검토가 필요하다.

검증 범위: 원본 해시, 경계값, 결측·0, OR 필터, 결정적 정렬, 설명 합계, 대안의 실제 인원, 입력·로딩 실패, 실제 브라우저 조작. 자동 검증과 브라우저 조작의 실행 결과 및 남은 검증 공백을 QA에 기록한다. 범위 조정 후에는 데스크톱 사용자 흐름과 제출 요건에 집중한다.

## 추천 설계 한 장

| 질문 | 결정과 근거 | 대안·검증·한계 |
|---|---|---|
| 무엇을 추천하는가 | 성과 최고가 아니라 **견적 확인할 우선 후보** | 구매·전환·실제 견적 데이터가 없어 성과 최적화 주장 불가 |
| 왜 조건과 순위를 나누는가 | 예산·분야·규모는 지켜야 할 조건, 나머지는 그 안의 비교 | 예산 초과 후보를 높은 점수로 상쇄하지 않음; 경계·OR·미확인 분리 검증 |
| 왜 집단 내 위치인가 | 플랫폼·규모 차이와 극단적 원시 조회수의 영향 완화 | 전체 집단 B와 보정 C의 1위가 14/76조건에서 달라짐. 품질 우월의 증거는 아님 |
| 왜 이 비중인가 | 우선 지표를 강조하되 운영 지표도 남기는 설명 가능한 가정 | 단일 지표 정렬·지표 제거·네 기준 1,504회 민감도 비교. 정확한 숫자는 검증된 최적값 아님 |
| 복잡한 보정은 꼭 필요한가 | C를 비교 맥락 설명 목적으로 선택 | B/C 상위 3명 평균 겹침 95.2%라 순위 효과는 제한적. 원지표 정렬을 함께 제공 |
| 평점·경험은 왜 같은 방식으로 변환하지 않는가 | 평점은 1~5 척도 유지, 경험은 추가 이력의 가산을 완만하게 | 균등 가중치도 분산이 달라 같은 영향이 아님. 평점 표본 수 없는 베이지안 보정 기각 |
| 실제로 무엇이 바뀌는가 | 기본 조건에서 참여율 우선은 서연리뷰66, 조회수 우선은 정은매거진77 | 참여율 8.1% 대 6.5%, 조회 10,003회 대 35,172회. 어느 쪽이 실제 광고 성과가 높은지는 모름 |
| 반대 사례는 있는가 | 뷰티·1만~10만·100만원의 조회수 우선에서 민준브이로그180(8,279회)이 지수뷰티151(10,012회)보다 앞섬 | 조회수만 최대화하는 정렬이 아님을 공개. 플랫폼 상대 위치·다른 지표가 함께 작용 |
| 이유도 검증하는가 | 집단·중간값 공유와 상대 후보별 기여 차이를 사용 | 기존 최대 기여 항목과 2위 대비 최대 차이가 29/47조건에서 달랐음. 총점 기여와 변별력 구분 |

[추천 설계 추가 진단](docs/LOGIC_REVIEW.md)에는 민감도·지표 제거·반례·설명 오류 수정 근거가 있다. 모집단은 더미 데이터이며 47조건 중 고유 후보 집합은 27개다. 독립적인 실제 사용자 표본으로 해석하지 않는다.

## 운영으로 발전시키려면

추가 수집: 현재 견적·작업 범위·사용권·일정, 콘텐츠별 광고 여부·관측 기간, 참여 지표 정의, 타깃 오디언스, 브랜드 적합성·안전성, 평점 응답 수·시점, 캠페인 목표와 실제 전환. 동의와 수집 권한을 전제로 한다.

향후 측정 계획: 조건 적용→후보 상세 확인→후보 선정 비율, 빈 결과율·복구율, 사용자 검토 시간, 견적과 평균 예산의 오차. 현재 구현에는 분석 수집·실사용 성과 측정을 넣지 않았다. 성과 라벨이 확보되면 수식과 가중치를 검증하며, 지금의 휴리스틱을 최적 모델처럼 다루지 않는다.

## 수익화: 반복 캠페인 운영의 가치 — 확장 설계, 미구현

첫 사용자의 후보 탐색·이유 확인·비교·메모는 모두 사용 가능하게 둔다. 결제·회원가입·가격표·Pro 잠금 화면은 이번 프로토타입에 구현하지 않는다. 한 번의 검색보다 **여러 캠페인의 후보·견적·선정 이유를 보관하고 팀과 검토하는 반복 업무**가 유료 가치라는 가설이다.

| 단계 | 고객이 얻는 가치 | 과금 가설 | 검증할 것 |
|---|---|---|---|
| 기본 탐색·체험 | 조건 탐색·추천 이유·후보 비교·메모 | 체험 기간과 무료 범위 미확정 | 업무를 완수할 수 있는가 |
| Pro | 캠페인별 기록·현재 견적·변경 이력·반복 보고 | 워크스페이스 구독, 운영 캠페인 규모 | 실제 재방문·복수 캠페인 수·지불 의향 |
| Team / Agency | 브랜드별 분리·공동 검토·승인·고객 공유 권한 | 좌석·브랜드 규모 | 팀 내 취합·승인 업무 비용 |

확장 UX: 비교 완료 → ‘캠페인 기록으로 보관’ 의도 발생 → “후보와 선정 이유를 캠페인별로 보관하고, 팀과 함께 검토하세요” → 가치 확인 또는 기본 메모 복사로 계속. 이 경로는 향후 설계이며 현재 앱에 동작하는 구매처럼 표시하지 않는다.

공개 가격 구조의 참고 근거는 [Modash](https://www.modash.io/pricing)와 [피처링](https://featuring.co/pricing)이다. 2026-09-21 확인한 공급사 자료이며 우리 서비스의 지불 의향을 입증하지 않는다. 가격은 데이터 갱신·AI 호출·지원 비용과 사용 빈도 확인 후 결정한다. 추천 순위 판매, 추천 이유를 유료로 숨기기, 비교 3열 제한을 보관 과금으로 바꾸기는 핵심 신뢰·업무 흐름과 충돌하므로 제외했다. 실제 유료 전환율·반복 사용·원가는 아직 측정하지 않았다.

## 온보딩 질문의 역할

첫 단계는 캠페인명(80자), 제품·전달할 메시지(1,500자), 목표다. 캠페인명·제품 메시지는 공백만으로 진행할 수 없다. 목표는 제품 알리기/관심·반응/구매 연결이며 조회수 우선/참여율 우선/균등을 시작점으로 제안한다. 이는 전환 예측이 아니라 탐색의 초깃값이다. 마지막 단계에서 프리셋 또는 비중을 사용자가 확인한다. 기존 설정 재진입 시 기존 기준을 보존하며, 목표를 새로 선택하면 해당 시작 프리셋으로 바뀐다.

두 번째 단계는 예산·분야·팔로워 조건이다. 세 번째에는 4개 프리셋과 슬라이더·숫자 입력을 기본 공개한다. 이전 단계로 돌아갈 때 브리프와 후보 조건을 유지한다. 기본 예산 200만원·뷰티/패션·마이크로는 체험용 초깃값이며 시장 표준이나 최적 예산이 아니다.

일정·제작 범위·사용권·연락처는 문의 준비 때, 관측 지표·출처·확인일은 성과 기록 때 받는다. 데이터가 없는 오디언스·브랜드 적합성을 충족하는 필터처럼 묻지 않는다. 질문별 근거와 경쟁사 참고는 [5차 UX 검토](docs/WORKFLOW_REVIEW.md)에 연결한다.

## 문의 이후의 협업 기록

문의 후보로 지정하면 왼쪽 **협업 관리**에 나타난다. 팀 검토안에서 선정 이유를 적은 다음 **문의 준비로 이동**할 수도 있다. 팀 검토안을 모든 경우에 강요하지 않고, 협업 관리로 바로 진입할 수 있다.

| 상태 | 다음 행동 | 저장 조건 |
|---|---|---|
| 문의 준비 | 기본 문안 수정·AI 다듬기·복사·이메일 초안 | 확인한 주소만 입력. 비어 있어도 문안 준비 가능 |
| 답변 대기 | 답변·견적 기록 | 실제 문의 날짜 필요 |
| 조건 협의 | 협업 조건 확정 | 실제 문의 날짜 필요 |
| 제작 진행 | 콘텐츠·성과 기록 | 문의 날짜·합의 비용·제작 범위 필요 |
| 집행 완료 | 성과 업데이트 | 문의 날짜·합의 비용·제작 범위 필요; 성과 공란은 미입력 표시 |
| 진행 안 함 | 기존 기록 보기 | 계약을 종료하거나 통보하는 외부 동작 없음 |

채널 링크·이메일·제작 범위·게시 예정일·사용권·합의 비용·협의 메모·수정한 문의 문안을 로컬에 저장한다. 채널 링크는 http(s), 이메일은 기본 형식을 검사한다. 원본에 없는 실제 연락처는 생성하지 않는다. 문의 문안의 수치·미확인 항목은 브리프와 입력한 조건에서 채운다. 복사는 발송이 아니며 이메일 초안 열기는 사용자의 메일 앱에 내용을 전달할 뿐이다. 상태와 실제 문의 날짜는 사용자가 직접 기록한다.

후보를 보관에서 빼도 저장한 협업 기록은 남는다. 조건 변경은 기존 협업을 새 캠페인으로 분리하지 않는다. 이 프로토타입은 단일 캠페인 작업실이다. 실제 운영의 복수 캠페인·공동 편집·발송·권한·백업은 별도 범위다.

## 성과 기록과 리포트

추천에 사용하는 CSV와 집행 후 기록을 분리한다. 한 크리에이터당 콘텐츠 한 건의 최신 누적값을 저장하며 새 측정 시 갱신한다. 시계열이나 여러 콘텐츠를 자동 합산하지 않는다.

- 입력: 콘텐츠 링크, 성과 확인일, 출처, 실제 집행비, 조회수, 좋아요, 댓글, 공유, 저장, 클릭, 구매, 연결 매출, 구매·매출 집계 기준.
- 숫자는 0 이상의 안전한 정수, 공란은 null. 합의 비용을 실제 비용으로 자동 대입하지 않는다. 실제 비용 0은 사용자가 확인하여 입력한 무상 집행이라는 뜻이며 원본의 미확인 예산 0과 다르다.
- 하나라도 수치를 입력하면 유효한 확인일과 출처가 필요하다. 구매·매출을 입력하면 집계 기준도 필요하다. 입력 날짜의 달력상 유효성을 검사한다.
- 비용 효율: CPV = 비용/조회수, CPE = 비용/(좋아요+댓글+공유+저장), CPC = 비용/클릭, CPA = 비용/구매. CPE는 네 반응 값이 모두 알려져야 한다. 입력 매출/집행비는 매출÷양수 비용×100이다. 이것은 입력 자료의 비율이며 인과적 광고 효과 검증이 아니다.
- 분모가 0 또는 필수 값이 공란이면 ‘—’. 비용 0·양수 조회수면 CPV 0은 유효하지만 비용 0의 매출 비율은 정의하지 않는다.
- 합계에는 지표별 입력 인원을 함께 표시한다. 전체 CPV는 비용과 양수 조회수가 함께 있는 후보끼리만 합산해 계산한다. 모든 비용을 일부 후보 조회수로 나누지 않는다.
- 원본 평균 조회수와 이번 콘텐츠는 측정 대상·기간이 달라 증감률·추천 정확도를 계산하지 않는다. 입력한 성과를 추천 가중치에 자동 반영하지 않는다.
- ‘예시 리포트’는 화면 확인용 합성 자료로 명시하고 실제 기록과 완전히 분리한다. 예시는 저장하지 않으며 내 기록으로 돌아갈 수 있다.

## 제품 내 AI — 선택적 연결 구현, 실제 모델 검증 대기

규칙 기반 추천은 브라우저에서 재현한다. LLM은 입력 정리와 문안·요약을 보조하며 순위를 계산하지 않는다. 기본 기능은 키 없이 실행된다.

| 위치 | 전달 자료 | 출력과 사용자 결정 |
|---|---|---|
| 브리프 정리 | 캠페인명·제품·목표·사용자 요청 | 메시지 초안·확인 질문. 직접 수정 후 제품 메시지에 적용; 1,500자 한도 |
| 문의 문안 다듬기 | 현재 문의 문안·추가 요청 | 편집할 초안·확인 질문. 적용 후 사용자가 직접 복사·발송 |
| 성과 요약 | 최대 8명의 입력 수치·출처·계산값 | 요약·추가 확인 질문. 전달 인원·제외 범위를 표시; 자동 재선정 없음 |

AI가 예산·비중·추천 대상·URL·현재 견적·성과를 만들어 자동 적용하지 못하게 구조를 분리했다. 스키마가 맞아도 내용의 사실성까지 보증되지는 않으므로 원자료 대조가 필요하다. 미지원 오디언스·브랜드 안전성·향후 구매 예측을 구현했다고 주장하지 않는다.

UI: 연결 상태 → 전달할 자료와 요청 확인 → 명시적 생성 → 실제 요청 중 표시 → 편집 → 사용자 적용. 미연결이면 연결 안내·요청문 복사·직접 입력을 제공한다. 실패·불완전·거절은 성공 초안으로 표시하지 않는다. 중단하거나 전달 자료가 바뀌면 이전 요청의 결과를 적용하지 않는다. 동시에 한 요청만 처리하고 자동 재시도로 비용을 늘리지 않는다.

로컬 Vite 서버의 /api/assistant를 통해 OpenAI Responses API를 사용한다. 키는 서버 환경변수에 두고 브라우저에 전달하지 않는다. 기본 모델은 구조화 출력을 지원하는 gpt-4.1-mini다. 45초 제한, 입력 크기·목적 검증, 고정 스키마, store:false를 사용한다. 이 로컬 연결은 인증 없는 공개 서비스 배포용 API가 아니다. static 배포에는 연결 필요 상태가 표시된다.

현재 미연결 화면·입력/응답 경계·오류 파서는 검증했다. 실제 API 키로 생성한 결과의 품질·비용·지연은 아직 검증하지 않았다. 제공사 정책상의 전체 데이터 보존 부재를 주장하지 않는다. 연결 후 허위 견적·성과 생성 여부, 원지표 유지, 사용자의 수정량·초안 완성 시간을 확인한다.

## 이번 개선의 수용 기준

| 행동·예외 | 기대 결과 | 증거 |
|---|---|---|
| 첫 기준 설정·재진입 | 3단계 브리프·조건·비중, 슬라이더 기본 공개, 저장 보존·취소 | P01·P03 브라우저 |
| 직접 비중 | 네 값 합계100, 미리보기=적용 순서, 취소·재방문 보존 | weights.test.ts·P03 |
| 문의 후보·보류 | 이유와 상태가 검토안에 반영, 문의 실제 발송 없음 | P04 브라우저·복사 |
| 후보 1명 | 비교 없이 상세에서 문의 후보→이유→검토안 | P05 브라우저 |
| 비용 미확인 문의 | 문의 가능하지만 예산·점수 보장 없음 | P06 브라우저·자동 검사 |
| 기준 변경 취소 / 적용 | 취소 시 원래 결과 유지, 적용 시 점수·이유·메모 함께 변경 | 브라우저 확인, 기존 4기준 조건 검사 |
| 네 번째 후보 담기 | 네 명 모두 보관, 비교만 최대 3명 | 저장 이전·선택 함수·브라우저 |
| 메모 작성 후 재방문 | 보관·비교 선택·메모 복원 | 복구 테스트·새로고침 확인 |
| 비용 미확인 / 조건 밖 후보 | 현 상태 표시, 순위 비교에서 제외 | 후보 상태·메모 테스트 |
| 표본 부족 | 점수와 설명이 같은 집단·중간값 사용 | 합성 20명 회귀 테스트 |
| 관측값 없는 후보 | 미평가·비용 미확인, 거짓 불일치 설명 없음 | 원본 신규 27명 검사·상세 브라우저 |

현재 실행 증거와 아직 남은 공개·사용자 검토 항목은 [QA](docs/QA.md)에 모은다.

### 5차 확장 수용 기준

| 행동·예외 | 기대 결과 | 검증 범위 |
|---|---|---|
| 문의 문안 복사·초안 열기 | 발송 완료 상태로 변경되지 않음 | 브라우저 복사·기본 문안 불변 테스트; 외부 메일 발송은 미실행 |
| 연락 후 상태·제작 진행 | 문의 날짜·합의 비용·제작 범위 없으면 안내 | 브라우저 오류 및 campaign.test.ts |
| 날짜·성과 저장 | 입력일과 원시 수치가 실제 저장·복원 | 브라우저 입력 및 저장 함수 검사 |
| 부분 성과·0 | 알려진 값만 합산, 분모 0은 계산 불가 | campaign.test.ts·상세 리포트 |
| 예시 리포트 | 별도 표시, 원본·실제 기록에 저장 안 됨 | 브라우저 왕복·fixture 분리 검사 |
| AI 미연결 | 생성 불가·설정/대체 경로, 가짜 생성 없음 | 브라우저 확인 |
| AI 응답 | 스키마·거절·미완료·입력 크기 처리 | assistant.test.ts; 실제 모델 출력은 미검증 |
| 실제 추천 처리 | Worker 대기 중 로딩, 실패·재시도, 이전 요청 폐기 | 코드 경로·실제 결과 브라우저; 임의 지연 없음 |
