# Creator Match · PRD

상태: 2026-09-22 벤치마크 2차 적용. 통합 적합 분석·근거·문의와 설명→한 화면 검토 온보딩을 구현했다. 캠페인별 후보·문의·상태의 분리·복원을 확인했다. 검색의 미확인 자료 처리, 캠페인별 성과 추가 검증, 사용자 최종 검토·제출은 남아 있다. [전체 계획](docs/BENCHMARK_APPLICATION_PLAN.md) · [온보딩 적용](docs/ONBOARDING_REVIEW.md) · [실행 증거](docs/QA.md).

## 문제와 사용자

1차 사용자는 사용자가 선택한 **브랜드 실무 마케터**다. 제품·예산을 바탕으로 후보를 추리고 견적을 문의하는 상황을 가정한다. ‘팀 승인·검토안 작성이 필요하다’는 이전 가정은 확인되지 않았고 사용자도 불필요하다고 지적하여 현재 흐름에서 제거했다.

핵심 업무는 **우리 캠페인에 왜 어울리는지 이해하고, 연락할 후보를 정해 협업을 이어가는 것**이다. 저장·비교는 선택 단계이며 한 명에게 곧바로 문의할 수 있다. 실제 인터뷰나 사람 대상 행동 관찰은 수행하지 않았다. [가설 페르소나·과업 검증](docs/PERSONA_TEST.md)에 지원자의 선택과 AI의 기능 점검을 구분한다.

공개 서비스의 탐색·분석·업무 연결 기능과 IAB의 측정 한계는 [리서치](docs/RESEARCH.md)에 기록했다. 이 자료는 문제의 맥락을 뒷받침하며 이 프로토타입의 수요나 성과를 입증하지 않는다.

## 목표와 범위

- 목표: 캠페인 설명 → 한 화면에서 제품·고객·조건·비중 검토 → 탐색·비교 → 문의 준비 → 협업 상태 → 성과 기록. 실제 발송·콘텐츠 확인은 외부에서 하고 확인한 내용만 기록한다.
- 성공 기준: 조건 위반 0, 미확인 가격의 무료 처리 0, 설명과 계산 일치, 빈 결과에서 다음 행동 가능.
- 비목표: 실제 ROAS 예측, 계약 자동 체결, 여러 명의 총예산 조합 최적화, 자동 연락·자동 수집·결제·로그인·서버 DB. 캠페인별 작업실의 수동 기록과 선택적 로컬 AI 연결을 제공한다. 다중 캠페인 경로의 추가 브라우저 검증은 남아 있다.
- 선택 가산 기능: 추천 근거 설명, 다섯 가지 정렬. 사용자 피드백 이후 추가: 첫 설정, 추천 기준 전환, 보관과 비교 선택 분리, 최대 3명 동시 비교, 직접 비중 조절, 캠페인 분석 시연, 개별 문의·발송별 내역·협업·성과 기록.
- 사용자 검증 전 가설: 설명을 보면 후보 검토에 대한 납득이 높아진다. 아직 시간 절감·만족도·전환율을 측정하지 않았다.

## 입력 정책

| 항목 | 정책 | 수용 기준 |
|---|---|---|
| 예산 | 1명·1회 캠페인 상한, 원화 양의 안전한 정수 | 빈 값·0·음수·소수·지수·문자 거절; 쉼표 표시·붙여넣기 허용; 동일 예산 포함 |
| 카테고리 | 10개 중 하나 이상, 다중 선택 OR | 선택 분야 중 하나만 같아도 통과 |
| 규모 | 한 구간 선택 | 나노 <10,000; 마이크로 10,000 이상 100,000 미만; 매크로 100,000 이상 |
| 적용 | 첫 설정 또는 조건 변경 창의 적용 버튼 | 취소·오류 시 기존 조건과 결과 유지 |

규모는 본 프로토타입의 운영 정의다. 단일 업계 표준으로 주장하지 않는다. 예산은 입력하면서 천 단위 쉼표를 표시한다. 입력 폼의 규모 이름은 팔로워 수를 먼저, 나노·마이크로·매크로는 보조로 배치한다.

## 데이터와 적격 판정

원본 200명·11필드. 파일 해시는 [데이터 감사](docs/DATA_AUDIT.md)에 기록한다. 원본 복사본을 그대로 읽고 별도 메모리 객체만 변환한다. CSV 구조·중복 ID·범주·숫자 범위를 먼저 검증하고 오류가 있으면 일부 데이터로 조용히 진행하지 않는다.

1. 카테고리 OR 및 규모 필터.
2. 캠페인 건수 >0이고 제공 평균 예산 >0이면 가격 참고값이 있는 후보.
3. 해당 후보의 평균 예산 ≤ 입력 예산이면 적격.
4. 가격 참고값이 있으나 입력 예산을 초과한 후보는 추천에서 제외하고 초과 인원에 집계한다.
5. 이력 또는 양수 평균 비용이 없는 후보만 동일 분야·규모의 ‘비용 미확인’으로 분리한다. 이들은 예산 적합을 보장하지 않으며 점수를 표시하지 않는다.

이력이 없는 27명의 0원은 무료가 아니다. 평점 공란은 미평가다. 과거 평균 예산은 현재 견적이 아니며 제작 범위·사용권 등에 따라 달라진다. 유이력 173명의 평균×건수와 누적액이 모두 불일치하므로 누적액으로 제공 평균을 덮어쓰지 않는다. 누적액은 점수에 쓰지 않는다.

참여율 7.7은 7.7%다. 조회수는 제공 평균 콘텐츠 조회수이며 광고 예상 조회수로 표현하지 않는다. 측정 기간·참여율 분모·평점 응답 수는 제공되지 않았다.

## 추천 수식

정책의 기준 파일은 `src/policy.ts`, 순수 계산은 `src/domain.ts`다.

캠페인 목표에 따라 조회수 우선/참여율 우선/균등 비교를 시작점으로 제안한다. 네 번째 단계에서 네 프리셋과 직접 비중을 공개하고 사용자가 확인·수정한다. 예시 캠페인 우회는 균등 비교다. 기존 35/35/20/10을 고정 기본값으로 보여주는 안은 지원자의 실제 UX 피드백으로 기각했다.

| 기준 | 참여율 | 조회수 | 평점 | 경험 |
|---|---:|---:|---:|---:|
| 참여율 우선 | 50% | 25% | 15% | 10% |
| 조회수 우선 | 20% | 55% | 15% | 10% |
| 협업 이력 우선 | 20% | 20% | 30% | 30% |
| 균등 비교 | 25% | 25% | 25% | 25% |

총점은 각 지표의 0~100 환산값에 선택한 가중치를 곱한 합이다. 가중치는 프리셋의 의미를 표현한 제품 가정이며 학습한 최적값이나 성과 확률이 아니다. 균등도 중립적인 정답이 아니다. 결과 상단의 기준·비중 변경 창에서 적용하면 동일 적격 집합의 순서만 달라지고 정렬은 추천순으로 돌아간다.

팔로워·예산·카테고리는 자격 조건이며 다시 가산하지 않는다. 팔로워·누적 예산 추가 가산은 규모의 중복 반영을 피하기 위해 제외했다. 새로운 프리셋 검증은 [우선순위 비교](docs/PRIORITY_EVALUATION.md)에 기록한다. 프리셋만 제공하는 것이 더 쉽다는 실제 사용자 근거는 없었다. 숙련 마케터의 통제권을 반영해 프리셋과 직접 조절을 함께 제공한다. 프리셋 값 자체와 자동 조절의 편의성은 별개 가설이다.

### 직접 비중 조절

- 네 지표 각각 0~100%, 정수 단위. 슬라이더 드래그·화살표/Home/End·숫자 입력을 제공한다.
- 지표 하나를 x%로 바꾸면 나머지 100−x%는 기존 세 지표의 상대 비율로 재분배한다. 다른 세 값이 모두 0이면 균등 분배한다. 한 번의 드래그·키보드 입력 중에는 조작을 시작한 비율을 유지해 반복 반올림에 따른 쏠림을 막는다.
- 정수화는 내림 후 소수 부분이 큰 순서로 잔여 1%를 배분한다. 동률은 참여율→조회수→평점→경험 순으로 고정한다. 합계는 항상 100%다. `src/weights.ts`가 단일 정책이다.
- 네 값과 재분배 안내를 함께 보여준다. 임의의 값 고정 기능은 이번 범위에서 제외한다.
- 결과 화면의 조절 창은 임시 비중만 편집한다. 적용 전 순위 미리보기는 제공하지 않는다. 적용 전까지 기존 결과를 유지하며 취소·닫기·Escape는 임시 변경을 버린다. 온보딩도 적용 후 결과를 보여준다.
- 적용하면 목록·상세·비교이 같은 사용자 비중을 쓴다. 조건 변경·대안 적용·새로고침은 직접 비중을 보존한다. 프리셋 버튼을 고르면 해당 프리셋으로 돌아간다.
- 0% 지표는 점수와 설명의 주요 근거에서 제외한다. 참여·조회가 모두 0%이면 평점·경험 중 실제 반영된 지표를 설명한다.
- 100% 선택도 허용한다. 특정 지표를 최대화하는 취향은 표현할 수 있지만 추천 정확도나 성과 최적성은 보장하지 않는다. 조회수 100%도 플랫폼·규모 내 상대 위치이므로 전체 원시 조회수 정렬과 다를 수 있다.

### 참여율·조회수

고정된 원본 전체에서 동일 플랫폼×규모 집단을 사용한다. 집단이 10명 미만이면 동일 플랫폼 전체, 그것도 10명 미만이면 전체 데이터로 확대한다. 비교 집단 이름·표본 수·확대 여부를 설명한다. 현재 집단은 13~69명이다.

값보다 작은 수를 L, 같은 수를 E, 집단 크기를 N이라 하면:
`상대 위치 = 100 × (L + (E−1)/2) / (N−1)`. N<2이면 50.
동점은 중간 위치다. 극단값의 크기가 아니라 순서를 사용해 이상치 영향을 줄이나 절대 차이를 잃는 단점이 있다. “상위 몇 %”라는 확률적 표현 대신 집단 내 상대 위치를 내부 계산에 사용한다. UI는 높은 편·중간 수준·낮은 편으로 풀어 쓴다. 사용자 필터가 바뀌어도 같은 후보 점수는 유지된다.

### 평점·경험

평점은 `(평점−1)/4×100`. 공란은 계산에만 척도 중간의 대체값 50을 사용하고 화면에는 ‘미평가’로 표기한다. 현재 공란 27명은 모두 견적 미확인 영역에 있어 종합 점수를 노출하지 않는다. 과거 이력이 있으나 평점이 없는 가상 테스트로 대체값 정책도 검증한다. 50은 평점 척도 3.0에 해당하며 현재 관측값 3.4~5.0보다 낮다. 불이익 없는 중립값이나 통계 추정값이 아니다. 향후 실제 결측이 적격 후보에 생기면 재분배·결측 분리안과 비교 검증해야 한다.

경험은 `log(1+건수)/log(1+전체 최대 건수)×100`. 최대 건수의 분모는 최소 1로 보호한다. 경험 증가의 가산을 체감시킨다. 캠페인 건수는 평점 응답 수가 아니므로 베이지안 평점 보정에 사용하지 않는다.

### 정렬·설명

추천순·참여율·평균 조회수·집행 건수·평점 내림차순. 평점 미평가는 마지막. 동점은 creator_id 오름차순. 계산·정렬은 반올림 전 값, 계산 결과는 코드와 실험 문서에서 확인한다. 제품 화면에는 총점·환산식·원본 ID를 노출하지 않는다.

비교표의 짧은 추천 근거는 `matchStory.ts`를 사용한다. 아래 항목은 이 짧은 설명의 정책이다. 9차 카드의 반복 문장은 제거했으며, 별도 분석 모달은 뒤의 ‘캠페인 적합 분석’ 명세를 따른다. 두 설명 모두 기존 점수 구성요소를 읽고 순위를 다시 결정하지 않는다.

- 반복 ‘중요’ 태그는 사용성 피드백에 따라 제거했다. 선택 기준은 결과 상단에서, 지표의 역할은 추천 문장에서 설명한다. 네 지표 동률은 균등이라고 설명한다. 대표 설명은 최고 비중 지표 중 환산값이 높은 하나를 선택한다.
- 참여·조회 환산값 75 이상은 ‘높은 편’, 50 미만은 ‘낮은 편’, 나머지는 ‘중간 수준’이라고 설명한다. 이는 읽기 위한 운영 구간이며 통계적 유의성이나 성과 검증값이 아니다. 정확한 상위 퍼센트를 주장하지 않는다.
- 평점은 실제 관측 유무를 먼저 확인한다. 미평가의 계산 대체값을 긍정 평가로 사용하지 않는다. 경험은 실제 집행 건수를 설명한다.
- 다른 양수 비중 지표에 강점이 있다면 `(환산값−50)×비중`이 가장 큰 한 개만 보조 근거로 덧붙인다. 0 비중은 대표 설명·보조 강점에서 제외한다.
- 선택한 분야, 1인당 예산과 참고 협업비, 더 확인할 행동을 별도로 설명한다. 예산 초과·미확인·현재 조건 밖 후보를 적격으로 포장하지 않는다.
- 브랜드 말투·시청자 취향·구매 가능성은 데이터로 알 수 없다. 브리프 텍스트를 분석해 그런 사실을 알아냈다고 쓰지 않는다. 채널 페이지는 콘텐츠 구성 제안을 보여주는 별도 로컬 페이지다.
- 비교표의 조회수·참여율은 플랫폼이 다를 때 높은 숫자만 우수하다고 강조하지 않는다. 원지표는 그대로 보여주고 설명은 각자의 비교 집단에 기반한다.
- 상세의 ‘어떻게 비교했나요?’에는 비교 집단·인원과 지표 사용을 평문으로 설명한다. 세부 수식·결측 대체·산술 불일치는 이 문서와 데이터 감사에 둔다.

견적 미확인 후보는 종합 점수 없이 평균 조회수 내림차순, 동률 ID순이다. 미래 광고 조회수·구매·수익의 예측으로 표현하지 않는다.

## 화면과 예외 정책

한국어 데스크톱 앱이다. 첫 방문은 캠페인 → 선택 타깃 고객 → 후보 조건 → 추천 기준의 네 화면으로 나눈다. 상단 **캠페인 설정**으로 재진입하며 취소하면 기존 조건·저장·협업 기록을 유지한다. 실제 로그인은 구현하지 않는다. 예시 우회는 뷰티·패션 / 1만~10만 미만 / 2,000,000원 / 균등 비교다.

카드는 **후보 식별 → 캠페인 적합 분석 버튼 → 원지표 → 참고 협업비·상세·문의** 순서다. 상세 지표에는 추가 원지표와 분석 진입 버튼이 있다. 분석 내용은 사용자가 요청할 때 별도 모달로 연다. 시연·프로토타입 배지는 사용자 요청에 따라 제품에서 제거하고 구현 경계는 제출 문서에 명시한다. 플랫폼은 아이콘과 접근 가능한 이름, 분야는 태그로 표시한다. 후보는 12명씩 더 볼 수 있다. 모바일 추가 구현·검증은 사용자 요청에 따라 제외한다.

후보 저장은 수 제한이 없다. **저장한 후보**에서 비교할 2~3명을 선택하면 선택한 인원 그대로 열을 만든다. 네 번째 선택은 기존 세 명을 유지하며 교체 방법을 안내한다. 저장 취소 시 비교에서도 빠진다. 조건 변경 후 저장한 후보에는 현재 조건 충족 여부를 다시 표시한다.

팀 검토안·문의/보류 별도 분류·필수 선정 메모를 제거했다. 비교표의 각 후보 머리글에 **문의하기**를 배치하며 카드·상세에서도 바로 갈 수 있다. 누른 한 명의 편집기만 열고 그 사람의 협업 기록만 생성한다. 기존 기록이 있으면 **협업 기록 이어보기**다. 내부 추천 지표나 이전 메모를 외부 문의 문안에 자동 포함하지 않는다.

공통 ‘다음 확인’ 문구는 제거했다. 실제 결측·예산 초과·낮은 지표를 분석 모달에서 설명하고, 타깃 고객은 확인해야 할 자료로 연결한다. 비용 미확인 후보도 문의할 수 있지만 무료·예산 충족으로 표시하지 않는다.

일반 주소는 localStorage에 캠페인별 조건·비중·저장·비교 ID·협업 기록을 보존한다. 콘텐츠 자료는 후보별 공통 자료다. 이전 v2/v3/v4 저장을 v5로 읽고 기존 메모·판단은 호환 목적으로 유지한다. 현재 UI는 별도 판단 상태를 요구하지 않는다. 보낸 문의는 발송별 문안·시각·시연/외부 기록 구분을 보존한다. 이전 한 건 기록은 읽을 때 이력으로 변환한다. 잘못된 저장 값은 제외하고 저장 제한은 안내한다.

**기준·비중 변경**에서 명시적으로 적용한 뒤에만 다시 계산한다. 즉시 전환 프리셋과 적용 전 순위 미리보기를 제거했다. 브라우저 구조화 입력 도구는 탐색 화면에서 명시적으로 적용하며 현재 사용자 비중으로 결과를 읽는다.

원본 11필드 중 ID는 내부 식별·동점 정렬·문서에 쓰고 화면에서 제거했다. 이름·플랫폼·분야·팔로워·조회수·참여율·집행 건수·평점·평균 비용은 카드와 상세, 누적 집행액은 상세에 표시한다. **채널 보기**는 새 탭의 채널 구성 제안 페이지를 연다. 원본에 없는 실제 SNS 계정을 만들어 연결하지 않는다.

| 상태 | 사용자에게 보여줄 것 | 다음 행동 |
|---|---|---|
| 로딩 | 데이터 불러오는 중 | 기다리기 |
| 로딩·CSV 오류 | 읽기 실패와 원인 안내 | 다시 불러오기 |
| 입력 오류 | 입력 창에 필드별 이유, 기존 결과 조건 유지 | 해당 입력 수정 |
| 분야·규모 후보 0 | 조합에 후보 없음 | 조건 변경 또는 가능한 규모 제안 적용 |
| 참고 예산 초과 | 예산 때문에 후보가 없는 이유, 같은 조건의 최소 참고 예산과 차액 | 변경값·예상 후보 수를 보고 적용 |
| 견적 미확인만 존재 | 예산 충족을 확인한 후보 없음 | 별도 후보 확인·조건 수정 |
| 적격 후보 있음 | 후보 수, 원지표, 과거 평균 비용, 상세의 추천 이유 | 상세 비교·재정렬 |

대안은 적격 후보가 0명일 때만 생성한다. 같은 분야·규모의 최소 평균 예산, 또는 예산·분야를 유지하는 다른 규모를 제시한다. 분야를 자동 변경하거나 예산 초과 후보를 적격 목록에 끼우지 않는다. 클릭 전에 정확한 변경과 후보 수를 보여준다.

## 검증과 평가

[비교 실험](docs/EVALUATION.md): 동일 적격 후보에 A 팔로워순 / B 전체 상대 위치 가중합 / C 플랫폼×규모 상대 위치 가중합을 적용. 현재 균등 비교로 재실행한 150개 조건 중 결과가 있는 76개에서 A/C 1위 차이 45개, B/C 14개. 조건 위반 0. 608회 가중치 ±20% 상대 변경에서 1위 35회 변경. 네 프리셋을 비교하면 76개 중 34개 조건에서 선택 기준에 따라 1위가 달랐다. 이러한 변화는 정확도 개선 증거가 아니다.

C는 비교 맥락을 설명할 수 있다는 이점 때문에 선택한다. 교차 플랫폼 측정을 완전히 동등하게 만들지는 못한다. 원지표 정렬을 함께 제공하고, 운영 시 카테고리·콘텐츠 형식·기간 차이 검토가 필요하다.

검증 범위: 원본 해시, 경계값, 결측·0, OR 필터, 결정적 정렬, 설명 합계, 대안의 실제 인원, 입력·로딩 실패, 실제 브라우저 조작. 자동 검증과 브라우저 조작의 실행 결과 및 남은 검증 공백을 QA에 기록한다. 범위 조정 후에는 데스크톱 사용자 흐름과 제출 요건에 집중한다.

## 추천 설계 한 장

| 질문 | 결정과 근거 | 대안·검증·한계 |
|---|---|---|
| 무엇을 추천하는가 | 성과 최고가 아니라 **견적 확인할 우선 후보** | 구매·전환·실제 견적 데이터가 없어 성과 최적화 주장 불가 |
| 왜 조건과 순위를 나누는가 | 예산·분야·규모는 지켜야 할 조건, 나머지는 그 안의 비교 | 예산 초과 후보를 높은 점수로 상쇄하지 않음; 경계·OR·미확인 분리 검증 |
| 왜 집단 내 위치인가 | 플랫폼·규모 차이와 극단적 원시 조회수의 영향 완화 | 전체 집단 B와 보정 C의 1위가 14/76조건에서 달라짐. 품질 우월의 증거는 아님 |
| 왜 이 비중인가 | 우선 지표를 강조하되 운영 지표도 남기는 설명 가능한 가정 | 단일 지표 정렬·지표 제거·네 기준 1,504회 민감도 비교. 정확한 숫자는 검증된 최적값 아님 |
| 복잡한 보정은 꼭 필요한가 | C를 비교 맥락 설명 목적으로 선택 | B/C 상위 3명 평균 겹침 95.2%라 순위 효과는 제한적. 원지표 정렬을 함께 제공 |
| 평점·경험은 왜 같은 방식으로 변환하지 않는가 | 평점은 1~5 척도 유지, 경험은 추가 이력의 가산을 완만하게 | 균등 가중치도 분산이 달라 같은 영향이 아님. 평점 표본 수 없는 베이지안 보정 기각 |
| 실제로 무엇이 바뀌는가 | 기본 조건에서 참여율 우선은 서연리뷰66, 조회수 우선은 정은매거진77 | 참여율 8.1% 대 6.5%, 조회 10,003회 대 35,172회. 어느 쪽이 실제 광고 성과가 높은지는 모름 |
| 반대 사례는 있는가 | 뷰티·1만~10만·100만원의 조회수 우선에서 민준브이로그180(8,279회)이 지수뷰티151(10,012회)보다 앞섬 | 조회수만 최대화하는 정렬이 아님을 공개. 플랫폼 상대 위치·다른 지표가 함께 작용 |
| 이유도 검증하는가 | 집단·중간값 공유와 상대 후보별 기여 차이를 사용 | 기존 최대 기여 항목과 2위 대비 최대 차이가 29/47조건에서 달랐음. 총점 기여와 변별력 구분 |

[추천 설계 추가 진단](docs/LOGIC_REVIEW.md)에는 민감도·지표 제거·반례·설명 오류 수정 근거가 있다. 모집단은 더미 데이터이며 47조건 중 고유 후보 집합은 27개다. 독립적인 실제 사용자 표본으로 해석하지 않는다.

## 운영으로 발전시키려면

추가 수집: 현재 견적·작업 범위·사용권·일정, 콘텐츠별 광고 여부·관측 기간, 참여 지표 정의, 타깃 오디언스, 브랜드 적합성·안전성, 평점 응답 수·시점, 캠페인 목표와 실제 전환. 동의와 수집 권한을 전제로 한다.

향후 측정 계획: 조건 적용→후보 상세 확인→후보 선정 비율, 빈 결과율·복구율, 사용자 검토 시간, 견적과 평균 예산의 오차. 현재 구현에는 분석 수집·실사용 성과 측정을 넣지 않았다. 성과 라벨이 확보되면 수식과 가중치를 검증하며, 지금의 휴리스틱을 최적 모델처럼 다루지 않는다.

## 수익화: 반복 캠페인 운영의 가치 — 확장 설계, 미구현

첫 사용자의 후보 탐색·이유 확인·비교·메모는 모두 사용 가능하게 둔다. 결제·회원가입·가격표·Pro 잠금 화면은 이번 프로토타입에 구현하지 않는다. 한 번의 검색보다 **여러 캠페인의 후보·견적·선정 이유를 보관하고 팀과 검토하는 반복 업무**가 유료 가치라는 가설이다.

| 단계 | 고객이 얻는 가치 | 과금 가설 | 검증할 것 |
|---|---|---|---|
| 기본 탐색·체험 | 조건 탐색·추천 이유·후보 비교·메모 | 체험 기간과 무료 범위 미확정 | 업무를 완수할 수 있는가 |
| Pro | 캠페인별 기록·현재 견적·변경 이력·반복 보고 | 워크스페이스 구독, 운영 캠페인 규모 | 실제 재방문·복수 캠페인 수·지불 의향 |
| Team / Agency | 브랜드별 분리·공동 검토·승인·고객 공유 권한 | 좌석·브랜드 규모 | 팀 내 취합·승인 업무 비용 |

확장 UX: 비교 완료 → ‘캠페인 기록으로 보관’ 의도 발생 → “후보와 선정 이유를 캠페인별로 보관하고, 팀과 함께 검토하세요” → 가치 확인 또는 기본 메모 복사로 계속. 이 경로는 향후 설계이며 현재 앱에 동작하는 구매처럼 표시하지 않는다.

공개 가격 구조의 참고 근거는 [Modash](https://www.modash.io/pricing)와 [피처링](https://featuring.co/pricing)이다. 2026-09-21 확인한 공급사 자료이며 우리 서비스의 지불 의향을 입증하지 않는다. 가격은 데이터 갱신·AI 호출·지원 비용과 사용 빈도 확인 후 결정한다. 추천 순위 판매, 추천 이유를 유료로 숨기기, 비교 3열 제한을 보관 과금으로 바꾸기는 핵심 신뢰·업무 흐름과 충돌하므로 제외했다. 실제 유료 전환율·반복 사용·원가는 아직 측정하지 않았다.

## 온보딩 질문의 역할

**새 캠페인 설명 → 초안 검토**의 두 단계다. 직접 입력하면 검토 화면부터 시작하고, 기존 캠페인 설정도 검토 화면에서 열린다.

| 위치 | 질문 | 사용하는 판단 |
|---|---|---|
| 설명 | 어떤 제품을 누구에게 소개할지 자유 문장 | 명시된 제품·고객·목표·조건을 초안으로 정리 |
| 검토 왼쪽 | 이름(80자)·제품(1,500자)·목표, 선택 고객(300자) | 캠페인 적합 문단·제작 제안·문의 |
| 검토 오른쪽 | 1명당 예산·분야·팔로워 구간 | 후보 자격 판정 |
| 선택 펼침 | 표현 방식·필수 요소(각500자), 고객의 선택 기준 | 제작 방향·확인할 질문 |
| 같은 검토 화면의 추천 기준 | 네 프리셋·드래그·숫자 비중 | 적격 후보의 순서 |

캠페인명·제품·예산·분야·팔로워는 유효한 값이 있어야 한다. 신규 예산·분야·팔로워는 빈 값이며 문장에서 명시된 값만 채운다. 타깃은 비워도 진행한다. 목표 미추출 시 ‘제품 알리기’ 시작값임을 안내한다. 제품 알리기/관심·반응/구매 연결에는 조회수/참여율/균등 기준을 제안하며 구매 확률을 계산하지 않는다.

**사용자가 고른 기준이 우선한다.** 아직 기준을 직접 고르지 않았다면 목표 변경에 맞춰 시작 프리셋을 바꾼다. 프리셋이나 직접 비중을 선택한 후에는 목표 변경·문장 재정리가 이를 덮어쓰지 않는다. 재정리는 문장에 없는 기존 타깃·표현·필수 요소와 수동 캠페인명을 유지한다.

금액은 ‘1명당/인당/한 명당’과 함께 쓴 값만 읽는다. 전체 예산을 나누지 않는다. 쉼표·만원 표현을 지원하되 0·음수·소수 원화·안전 범위 밖 값은 검증 오류로 남긴다. 일반 자연어 이해 모델이 아니라 로컬 규칙이므로 추출 결과는 수정 가능하게 둔다.

고정 하단에서 현재 추천 기준과 미선택 조건을 확인한다. 비중 조절 바로가기는 슬라이더로 초점도 이동한다. 오류는 첫 필드로 초점을 옮기고 입력을 보존한다. 이전 이동은 현재 폼을 유지하며 설정 취소는 저장된 캠페인을 바꾸지 않는다. 미완료 폼의 새로고침·캠페인 전환 복원은 지원하지 않는다. 일정·제작 수량·사용권·연락처는 문의 때, 성과 수치·출처는 집행 후에 받는다.

완료 전환은 4.2초 동안 분야·팔로워 → 예산·비용 미확인 분리 → 선택 기준의 순서로 안내한다. 1.4초마다 강조만 바뀌는 표현 시간이며 실제 검색 작업의 진행률을 측정한 것이 아니다. 일반 조건 변경에는 이 지연을 추가하지 않으며 실제 CSV·Worker 로딩과 실패·재시도는 별도다.

## 캠페인 적합 분석

목록의 작은 분석 버튼, 상세, 비교표는 같은 통합 모달을 연다. 적합 분석·콘텐츠·광고 반응·협업 이력·고객 도달 자료 탭으로 구성한다. 요약과 별도의 리포트를 다시 생성하지 않고 하나의 `CampaignReview` 결과를 공유한다.

| 분석 영역 | 산출 근거 | 수용 기준·한계 |
|---|---|---|
| 첫 문단 | 캠페인 제품·목표·고객 + 연결된 게시물·검토 댓글; 자료가 없으면 CSV 근거 | 3~4문장. 후보의 표현과 제품 활용, 조율할 점을 구분. 실제 시청자 직업을 만들지 않음 |
| 근거 버튼 | 최대 두 게시물과 검토된 제품 질문 댓글; 없으면 원지표 | 같은 창에서 해당 원문으로 이동. 존재하지 않는 근거를 인용한 모델 응답은 거절 |
| 예산 | 사용자 상한과 과거 평균 비용 | 초과·미확인 상태를 적합으로 포장하지 않음 |
| 제안·문의 질문 | 제품과 필수 노출 요소, 확인되지 않은 타깃 자료·제작 범위 | 필요한 질문만 선택. 내부 점수·평가 문단은 외부 문의에 넣지 않음 |
| 채널 지표 | 기존 점수 구성요소의 실제 관측값 | 접힌 보조 근거. 미평가를 긍정 평가로 쓰지 않음 |
| 리포트 | 화면에 표시된 최종 문단·근거·제안·질문 | 같은 문단을 내보내며 별도로 재생성하지 않음. 복사 확인, 파일 다운로드는 앱 안 브라우저에서 미확인 |

`review.ts`가 분석 결과·캐시·문의·리포트의 공통 기준이다. 캠페인·비중·콘텐츠가 바뀌면 기존 캐시를 재사용하지 않는다. `domain.ts`의 추천 필터·수식·순위는 분석으로 변경하지 않는다. 실제 모델을 연결해도 순위는 같은 순수 함수가 결정한다.

추가 콘텐츠는 5명의 별도 편집 예시다. 실제 SNS 수집이 아니며 원본 CSV와 별개다. 자료 출처는 상세와 리포트에서 확인할 수 있다. 콘텐츠가 맞아 보여도 실제 고객 도달은 미확인일 수 있다. 자료가 없는 후보의 기본 순위를 낮추지 않는다.

예: 민준브이로그180의 비교 리뷰는 발색·사용 후 변화 설명으로, 시우매거진155의 선명한 메이크업은 일상 표현으로 조율할 질문으로 연결한다. 둘 모두 실제 직장인 시청자를 확인했다고 설명하지 않는다. 예시 콘텐츠의 해석을 실제 시장 관측으로 전환하지 않는다.

**제안 담아 문의하기**는 신규 초안에 제작 방향·선택 질문만 넣는다. 기존 기록에는 **기존 문의에 제안 추가** 미리보기를 먼저 보여주고 확인 후 기존 문안 뒤에 붙인다. 동일한 제안을 중복 추가하지 않는다. 기존 진행 단계·성과·발송 이력은 유지하며 필요하면 추가 문의 편집기를 연다.

### 자료 등록

제목·게시일·캡션은 필수 `*`, 주소·조회수·댓글은 선택이다. 날짜·URL·숫자·자료 형태를 검사한다. 접두사 없는 주소는 https로 보완하고 미입력 조회수는 null로 저장한다. 분류한 댓글만 반응 통계에 포함하며 이후 새로 붙인 문장은 미분류로 보존한다. 등록한 첫 게시물부터 해당 후보의 예시 자료 대신 사용자 자료만 사용한다. 후보당 최대 200개, 게시물당 검토 댓글 최대 50개다. 저장 실패 시 기존 자료·입력을 유지한다.

광고와 일반 콘텐츠 평균은 각각 조회수가 있는 게시물만 분모로 쓴다. 등록 게시물 수와 조회수가 확인된 수를 구분한다. 구매 의향은 실제 구매가 아니다. 등록 데이터는 원본 CSV·점수에 합치지 않는다.

경쟁사별 연결과 남은 검증은 [적용 결과](docs/UNIFIED_ANALYSIS.md)에 기록했다. 유료 제품 직접 사용·정확도 우월성 검증·사람 대상 사용성 검증은 하지 않았다.

## 문의 이후의 협업 기록

문의 대상 한 명의 편집기를 바로 연다. 팀 검토안이나 추가 대상 지정은 없다. 진행 관리·보낸 문의·성과 기록 탭으로 분리한다.

| 상태 | 지금 입력·확인할 것 | 기본 다음 행동 |
|---|---|---|
| 문의 준비 | 수신 대상 한 명, 문의 문안; 선택 제작 범위·일정·사용권 | 문의 보내기 |
| 답변 대기 | 보낸 문안, 문의 시각, 진행 메모 | 답변 받았어요 → 조건 협의 |
| 조건 협의 | 받은 견적·합의 비용 구분, 제작 범위·게시일·사용권 | 협업 확정하기 → 제작 진행 |
| 제작 진행 | 최종 조건·일정·진행 메모 | 게시 완료로 변경 → 집행 완료 |
| 집행 완료 | 콘텐츠 링크·확인일·출처·비용·조회수, 추가 반응·구매 | 저장하고 성과 리포트 보기 |
| 진행 안 함 | 진행하지 않은 이유, 이전 조건·성과 보존 | 조건 협의로 다시 검토 |

목록의 상태 선택은 즉시 저장한다. 편집기의 상태 선택은 해당 단계 필드를 보여주며 저장 버튼으로 반영한다. 다음 행동 버튼은 기록을 저장하고 다음 상태로 이동한다. 모든 상태로 앞뒤 전환 가능하며 이미 입력한 필드·보낸 문안·성과를 삭제하지 않는다. 누락된 문의 날짜·견적·제작 조건은 `stageNeeds`가 ‘나중에 추가할 항목’으로 안내하고 상태 기록을 막지 않는다. 다만 입력한 음수·잘못된 날짜·URL·이메일과 성과의 출처 누락은 저장을 막는다. ‘단계가 바뀜’과 ‘모든 자료가 준비됨’을 구분한다.

받은 견적(`quotedCost`)과 합의 비용(`agreedCost`)은 별도다. **받은 견적으로 채우기**를 누를 때만 복사하며 실제 집행비에는 자동 대입하지 않는다. 값이 공란이면 미입력, 직접 입력한 0원은 확인한 값이다.

**문의 보내기**는 문안·시연 시각을 `sentHistory`에 한 건씩 저장한다. 첫 문의는 답변 대기로 바꾸며 추가 문의는 진행 중인 협업 상태를 유지한다. 보낸 직후 **보낸 문의** 탭을 자동으로 열고 문안 전체를 보여준다. 왼쪽 보낸 문의 메뉴와 협업 목록에서도 다시 열 수 있다. 메뉴 숫자는 후보 수가 아닌 문의 건수다. **추가 문의 작성**으로 기존 단계 변경 없이 다시 보낼 수 있다. 실제 발송·실제 문의 날짜는 만들지 않는다. 실제 이메일·DM으로 보낸 경우 ‘외부에서 보낸 문의 기록’에서 실제 날짜를 입력해 답변 대기로 바꾼다. 문안 복사와 이메일 앱 초안 열기는 상태를 바꾸지 않는다. 실제 발송 API·수신함·배달 확인은 없다. 수정 중 문안과 각 발송 문안은 분리하여 이후 수정·재발송으로 이전 기록이 바뀌지 않는다. 날짜 입력이나 상태 변경만으로 발송 기록을 만들지 않는다. 외부 문의는 기록 버튼을 눌러 저장한다.

협업 기록은 creator_id별로 분리해 저장한다. 선택한 다른 후보의 기록에는 영향이 없다. 닫을 때 미저장 변경이 있으면 계속 작성/저장하지 않고 닫기/저장하고 닫기를 제공한다. 연락처는 직접 확인해 입력하며 합성 연락처를 실제 주소처럼 만들지 않는다.

후보를 보관에서 빼도 저장한 협업 기록은 남는다. 조건 변경은 기존 협업을 새 캠페인으로 분리하지 않는다. 캠페인 선택으로 작업실을 전환할 수 있다. 공동 편집·외부 발송·권한·백업은 별도 범위다. 두 캠페인의 같은 후보에 서로 다른 문의 문안·상태를 저장하고 전환·새로고침 후 보존을 확인했다. 서로 다른 성과 수치를 기록하는 전체 경로의 추가 검증은 남아 있다. 상세 상태별 설계와 남은 검토는 [7차 UX 검토](docs/COLLABORATION_UX.md)에 기록한다.

## 성과 기록과 리포트

추천에 사용하는 CSV와 집행 후 기록을 분리한다. 한 크리에이터당 콘텐츠 한 건의 최신 누적값을 저장하며 새 측정 시 갱신한다. 시계열이나 여러 콘텐츠를 자동 합산하지 않는다.

- 입력: 콘텐츠 링크, 성과 확인일, 출처, 실제 집행비, 조회수, 좋아요, 댓글, 공유, 저장, 클릭, 구매, 연결 매출, 구매·매출 집계 기준.
- 숫자는 0 이상의 안전한 정수, 공란은 null. 합의 비용을 실제 비용으로 자동 대입하지 않는다. 실제 비용 0은 사용자가 확인하여 입력한 무상 집행이라는 뜻이며 원본의 미확인 예산 0과 다르다.
- 하나라도 수치를 입력하면 유효한 확인일과 출처가 필요하며 해당 라벨에 `*`와 required 속성이 표시된다. 구매·매출을 입력하면 집계 기준도 필수 표시한다. 0도 입력값으로 간주한다. 아무 수치도 없으면 확인일·출처는 선택이다. 입력한 날짜는 달력상 유효성을 검사한다.
- 필수 표시: 캠페인명·제품·목표·예산·분야·팔로워 구간, AI 초안 설명·필수 초안 필드. 타깃 고객은 선택. 외부 문의 기록 행동에는 실제 문의 날짜가 필요하며 해당 구역에 표시한다. 상태 변경 자체의 선행 필수값은 아니다.
- 콘텐츠·채널 링크는 `https://` 없이 붙여 넣어도 저장 시 보완한다. `http://`도 허용한다. 실행 프로토콜·사용자정보 포함 URL·상대 경로·잘못된 주소는 거절한다. 입력 필드는 URL 자동 유효성 검사가 http 접두사를 강제하지 않도록 구성한다.
- 저장 실패는 ‘아직 저장하지 못했어요’와 개별 필드 오류로 알린다. 오류가 있는 탭·펼침을 열고 첫 필드로 초점을 옮긴다. 입력한 내용은 유지한다.
- **저장하고 성과 리포트 보기**는 유효성 검사→저장→편집창 닫기→리포트 표시 순서다. 이미 리포트 화면에서 수정한 경우에도 창을 명시적으로 닫는다. 리포트의 수치는 즉시 갱신한다.
- 비용 효율: CPV = 비용/조회수, CPE = 비용/(좋아요+댓글+공유+저장), CPC = 비용/클릭, CPA = 비용/구매. CPE는 네 반응 값이 모두 알려져야 한다. 입력 매출/집행비는 매출÷양수 비용×100이다. 이것은 입력 자료의 비율이며 인과적 광고 효과 검증이 아니다.
- 분모가 0 또는 필수 값이 공란이면 ‘—’. 비용 0·양수 조회수면 CPV 0은 유효하지만 비용 0의 매출 비율은 정의하지 않는다.
- 합계에는 지표별 입력 인원을 함께 표시한다. 전체 CPV는 비용과 양수 조회수가 함께 있는 후보끼리만 합산해 계산한다. 모든 비용을 일부 후보 조회수로 나누지 않는다.
- 원본 평균 조회수와 이번 콘텐츠는 측정 대상·기간이 달라 증감률·추천 정확도를 계산하지 않는다. 입력한 성과를 추천 가중치에 자동 반영하지 않는다.
- ‘예시 리포트’는 화면 확인용 합성 자료로 명시하고 실제 기록과 완전히 분리한다. 예시는 저장하지 않으며 내 기록으로 돌아갈 수 있다.

## 제품 내 AI — 기본 로컬 동작과 선택적 모델 연결

| 위치 | 현재 구현 | 사용자가 결정할 것 |
|---|---|---|
| 온보딩 | 로컬 규칙으로 캠페인 문장에서 조건 초안 추출, 0.9초 표현 시간 | 추출값·시작값을 확인하고 수정 |
| 자연어 검색 | 편집 가능한 포함·제외 키워드, 선택적 실제 모델 연결 | 키워드를 확인·수정한 뒤 적용 |
| 참고 이미지 | 로컬 색감 추출 또는 연결된 모델의 이미지 해석 | 제안 태그를 검토. 색감만으로 맥락·인물 특성을 추정하지 않음 |
| 적합 분석 | 제공 근거에 따른 로컬 결과 또는 연결된 모델 결과 | 근거와 질문 검토, 문의 적용 |
| 문의·성과 | 기존 로컬 문안 수정·입력 성과 요약 | 직접 편집·적용·복사, 발송은 별도 |

`server/research.ts`는 검색·후보 분석의 선택적 모델 연결이다. 서버 키가 설정되면 사용자 요청에 따른 캠페인·근거 자료·선택 이미지를 모델에 전달한다. 키는 클라이언트에 노출하지 않고 서버에서만 사용하며 입력 크기·45초 제한·응답 형태·근거 ID를 확인한다. 근거 ID 확인은 모델 문장 자체의 정확성을 보장하지 않는다.

이번 환경은 키가 없어서 실제 모델 응답을 검증하지 않았다. 키가 없거나 정적 호스팅인 경우 로컬 분석을 사용한다. 설정된 모델 요청이 실패하면 오류와 재시도·확인된 자료 보기 경로를 제공한다. 닫거나 자료가 바뀌면 진행 요청을 취소한다. 분석에는 이전 2.1초 인위적 지연이 없고 실제 요청 대기 중에만 로딩한다. 브리프·문의·성과 0.9초와 온보딩 완료 4.2초는 기존 표현 시간이다.

브리프는 전체 예산을 1인당으로 바꾸지 않는다. 문장에 없는 신규 예산·분야·규모는 빈 값으로 두고 검토 화면에서 받는다. 일반 자연어·부정 표현을 완전히 이해하는 모델처럼 설명하지 않는다. 제품 화면의 원자료 JSON·환경변수·시연 배지는 노출하지 않되 자료 출처와 해석 범위는 확인할 수 있게 둔다.

일반 주소는 localStorage, `?demo`는 별도 sessionStorage를 사용한다. 체험 탭에서도 새로고침 후 기록이 유지되지만 일반 캠페인 기록을 읽거나 덮지 않는다. 로그인·클라우드 저장·다른 기기 동기화는 없다.

## 현재 수용 기준 · 벤치마크 2차 적용

| 행동·예외 | 기대 결과 | 증거 |
|---|---|---|
| 설명→한 화면 검토 | 명시한 조건 반영, 없는 필수값 보완, 타깃 미정 진행, 수동 이름·비중 보존 | setup.test.ts 14개·2차 데스크톱 |
| 캠페인 분리 | 신규 후보 목록 초기화, 각 캠페인의 문안·상태 복원 | 2차 전환·새로고침 확인; 성과 추가 검증 남음 |
| 비중 수정·취소·적용 | 합계100, 취소 시 기존 결과 유지, 적용 후 순위 변경; 미리보기 없음 | weights.test.ts·8차 브라우저 |
| 온보딩 완료 | 실제 입력 조건을 포함한 3단계 안내 후 후보 | MatchingTransition·8차 화면 관찰 |
| 통합 분석 | 같은 결과로 문단·근거·문의·리포트 연결, 기존 문의 보존 | review.test.ts·contentValidation.test.ts·이번 데스크톱 검증 |
| 추천 근거 | 실제 비중·점수 구성과 일치, 결측·현재 조건 예외 표시 | matchStory.test.ts·domain.test.ts |
| 후보 1명 또는 비교3명 | 선택한 인원 그대로 비교, 누른 한 명에게 문의 | 8차 비교표·편집기 확인 |
| 보낸 문의 | 발송 직후 기본 노출, 별도 메뉴, 추가 문의에도 이전 문안 보존 | sentHistory.test.ts·8차 3건 확인 |
| 상태·성과 | 상태 이동·되돌림, 올바른 값·출처 검증, 부분 집계 | campaign.test.ts·7/8차 브라우저 |
| 채널 구성 제안 | 별도 페이지, 콘텐츠를 제안으로 표시, 추천 화면 상태 유지 | 기존 링크 구조 유지 |
| AI·성과 시연 | 동작하는 입력·결과·적용, 실제 모델·광고 성과와 구분 | 기존 자동 검사·7/8차 브라우저 |
| 성과 저장 UX | 조건부 필수 *, 접두사 없는 URL, 누락 초점·펼침, 같은 리포트의 편집창 닫힘 | campaign.test.ts·9차 브라우저 |
| 실패·원본 | 오류와 재시도, CSV 200행·해시 보존 | loadData.test.ts·domain.test.ts·QA |

현재 실행 증거와 아직 남은 사용자 검토·공개·제출 항목은 [QA](docs/QA.md)와 [요구사항 추적표](docs/REQUIREMENTS.md)에 모은다. 이전 팀 검토안·적용 전 미리보기 정책은 현재 구현에서 제거되었다.
