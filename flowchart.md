# 사용자 여정과 추천 흐름

정책은 [PRD](PRD.md), 사용자 피드백과 정보 구조는 [UX 검토](docs/UX_REVIEW.md), 계산은 `src/domain.ts`, 화면은 `src/App.tsx`에 연결된다.

```mermaid
flowchart TD
    A["데모 진입 / 서비스에서는 가입 완료 직후"] --> B{"유효한 로컬 조건 있음?"}
    B -- 아니오 --> C["첫 설정: 프리셋 또는 직접 비중 / 합계100"]
    C --> D["분야·예산·팔로워 조건"]
    B -- 예 --> E["저장 조건으로 탐색"]
    D --> F{"입력 유효?"}
    F -- 아니오 --> G["필드 오류 / 수정"]
    G --> D
    F -- 예 --> E
    E --> H["CSV 로딩·구조·값 검증"]
    H --> I{"데이터 정상?"}
    I -- 아니오 --> J["오류 안내 / 다시 불러오기"]
    J --> H
    I -- 예 --> K["분야 OR·팔로워 구간 필터"]
    K --> L{"이력과 양수 평균 비용 있음?"}
    L -- 아니오 --> M["비용 미확인 탭 / 조회수순 / 점수 없음"]
    L -- 예 --> N{"평균 비용이 예산 이하?"}
    N -- 아니오 --> O["예산 초과 제외"]
    N -- 예 --> P["고정 플랫폼·규모 집단"]
    P --> Q{"표본 10명 이상?"}
    Q -- 아니오 --> R["플랫폼 전체 → 전체 데이터로 확대"]
    Q -- 예 --> S["참여율·조회수 상대 위치"]
    R --> S
    S --> T{"평점 있음?"}
    T -- 아니오 --> U["미평가 / 계산 대체값 50"]
    T -- 예 --> V["평점 환산"]
    U --> W["경험 로그 환산 + 선택 기준의 가중합"]
    V --> W
    W --> X{"적격 후보 있음?"}
    O --> X
    M --> X
    X -- 아니오 --> Y["빈 원인과 대안 / 변경값·예상 인원"]
    Y --> Z{"사용자가 대안 적용?"}
    Z -- 예 --> F
    Z -- 아니오 --> Y
    X -- 예 --> AA["추천 목록 / 원지표와 주요 근거"]
    AA --> RE["캠페인 설정으로 재진입"]
    RE --> C
    RE -- 변경 없이 돌아가기 --> AA
    AA --> AB["프리셋 즉시 전환"]
    AB --> W
    AA --> WC["직접 비중 조절 / 합계100 / 상위3 미리보기"]
    WC -- 취소 --> AA
    WC -- 적용 --> W
    AA --> AI["재정렬 / 기존 점수 유지"]
    AI --> AA
    AA --> AC["상세: 판단 근거·전체 데이터·추가 계산"]
    AC --> AD["후보 보관 / 3명 제한 없음"]
    M --> AD
    AA --> AD
    AD --> AJ["보관 목록에서 비교할 2~3명 선택"]
    AJ --> AE["현 조건·원지표·상대 강점과 한계 비교"]
    AE --> DS["검토 중 / 문의 후보 / 보류 지정"]
    AC --> DS
    DS --> HK{"문의 후보 1명 이상?"}
    HK -- 아니오 --> HS["문의 후보 지정 안내 / 검토로 복귀"]
    HS --> AD
    HK -- 예 --> AK["팀 검토안 / 후보별 문의 이유 작성"]
    AK --> HR{"모든 문의 이유 작성?"}
    HR -- 아니오 --> AK
    HR -- 예 --> AF["문의 후보·이유·기준·한계 / 검토안 복사"]
    AF -- 복사 실패 --> HC["텍스트 선택 / 직접 복사"]
    AF --> AG["팀 확인 후 실제 채널·견적 문의 / 데모 범위 밖"]
    HC --> AG
    AA --> AH["조건 변경 창"]
    AH --> F
```

CSV 요청은 앱이 열릴 때 시작한다. 차트는 사용자가 마주하는 단계와 논리 분기를 표현한다. 기준 변경은 필터를 바꾸지 않고 점수·설명을 재계산한다. 재정렬만 바꾸면 점수는 유지되며 선택 지표 내림차순·동점 ID순이다. 조건 변경·대안 적용 후에는 추천순으로 돌아간다. 가격 미확인 후보에는 점수를 노출하지 않는다.

보관 목록·비교 선택·문의/보류 상태·메모·사용자 비중은 조건 변경과 재방문에도 보존한다. 후보를 빼면 비교 선택과 판단 상태에서도 제외한다. 문의 후보 한 명이면 상세에서 바로 검토안으로 이동한다. 네 번째 비교 선택은 기존 세 명을 유지하고 안내한다. 기준 안내 모달은 임시 선택 → 적용으로 동작하며 취소하면 기존 기준을 유지한다. 상단 빠른 전환은 즉시 적용한다.

가입·로그인·외부 문의는 구현 범위 밖이며 실제로 수행하는 것처럼 표시하지 않는다. 모달은 닫기·Escape로 원래 화면에 돌아온다. 저장 오류가 나도 현재 탐색을 계속할 수 있다.


## 향후 확장 흐름 — 현재 앱에 미구현

아래는 PRD의 역할 분리와 예외 정책을 설명하는 설계다. 실제 AI·결제·팀 저장을 제공하는 화면이 아니다.

```mermaid
flowchart LR
    A["직접 조건 입력"] --> E["조건 검증 / 고정 추천 함수"]
    B["선택: 브리프 입력"] --> C["AI 조건 초안 / 근거 / 미지원·질문"]
    C --> D{"사용자가 확인·적용?"}
    D -- 예 --> E
    D -- 취소·실패 --> A
    E --> F["후보 비교 / 판단 기록"]
    F --> G["기본 메모 복사"]
    F --> H["캠페인별 보관·팀 검토 필요"]
    H --> I["Pro 가치 안내 / 가격·범위 확인"]
    I -- 닫기 --> G
    I -- 추후 선택 --> J["향후 캠페인 기록·권한 설계"]
```
