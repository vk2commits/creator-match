# 사용자 여정과 추천 흐름

벤치마크 첫 적용 기준(2026-09-22). 정책은 [PRD](PRD.md), 화면별 판단은 [제품 전체 검토](docs/PRODUCT_UX_AUDIT.md)에 연결한다.

```mermaid
flowchart TD
    A["진입"] --> B{"현재 캠페인에 저장한 조건 있음?"}
    B -- 없음 --> C["1 캠페인: 이름·제품·목표"]
    C --> BA{"입력 방식"}
    BA -- 직접 입력 --> TC["2 타깃 고객: 선택 입력"]
    BA -- AI 초안 --> AI["문장 정리 → 편집 가능한 초안 확인"]
    AI --> TC
    TC --> D["3 후보 조건: 예산·분야·팔로워"]
    D --> F{"입력 유효?"}
    F -- 아니오 --> G["필드 오류 수정"]
    G --> D
    F -- 예 --> WT["4 추천 기준: 프리셋·직접 비중 합계100"]
    WT --> TR["4.2초 안내: 조건 → 예산 → 추천 기준"]
    TR --> E["추천 결과 준비"]
    B -- 있음 --> E
    E --> H["CSV 구조·값 검증"]
    H --> I{"데이터 정상?"}
    I -- 아니오 --> J["읽기 오류·재시도"]
    J --> H
    I -- 예 --> WL["Worker 추천 요청·실제 대기 표시"]
    WL -- 실패 --> WE["계산 오류·재시도"]
    WE --> WL
    WL --> K["분야 OR·팔로워 구간 필터"]
    K --> L{"이력·양수 평균 비용 있음?"}
    L -- 없음 --> M["비용 미확인 분리·조회수순"]
    L -- 있음 --> N{"과거 평균 비용 ≤ 예산?"}
    N -- 아니오 --> O["예산 초과 제외"]
    N -- 예 --> P["고정 플랫폼×규모 집단"]
    P --> Q{"10명 이상?"}
    Q -- 아니오 --> R["플랫폼 전체 → 전체 데이터 확대"]
    Q -- 예 --> S["참여율·조회수 상대 위치"]
    R --> S
    S --> T{"평점 있음?"}
    T -- 없음 --> U["화면 미평가·계산 대체값50"]
    T -- 있음 --> V["평점 환산"]
    U --> W["경험 로그 환산·선택 가중합"]
    V --> W
    W --> X{"적격 후보 있음?"}
    M --> X
    O --> X
    X -- 없음 --> Y["빈 원인·대안의 변경값과 후보 수"]
    Y -- 사용자 적용 --> E
    X -- 있음 --> AA["목록: 원지표·콘텐츠·비용·분석 버튼"]
    AA --> WC["기준·비중 수정: 임시 값만 편집"]
    WC -- 적용 --> WL
    WC -- 취소 --> AA
    AA --> SO["재정렬: 점수 유지·동점 ID순"]
    SO --> AA
    AA --> AN["분석 요청 → 로딩 → 근거·제안 모달"]
    AN -- 제안 담아 문의 --> ONE
    AA --> AC["상세 지표와 캠페인 분석 버튼"]
    AC --> CH["채널 보기: 별도 예시 페이지"]
    AC --> ONE["선택한 한 명에게 문의"]
    AA --> ONE
    AA --> AD["후보 저장"]
    M --> AD
    AD --> AJ["저장 목록에서 2~3명 비교 선택"]
    AJ --> AE["선택한 인원 그대로 비교표"]
    AE -- 후보별 문의 --> ONE
    AA --> CF["조건 변경·유효성 검사"]
    CF -- 적용 --> E
    AA --> RE["캠페인 설정 재진입"]
    RE --> C
    RE -- 취소 --> AA
```

CSV는 앱 진입 시 병렬로 불러온다. 위 차트는 논리 분기와 사용자 동선을 설명한다. 4.2초는 입력 조건과 절차를 읽는 **표현 시간**이며 실제 처리율이 아니다. 일반 필터 변경에는 이 지연을 넣지 않는다. 실제 로딩·Worker 오류는 별도 처리한다.

비중 변경 전에는 결과가 바뀌지 않으며 적용 전 순위 미리보기는 없다. 조건 변경·대안 적용 후에는 추천순으로 돌아간다. 비용 미확인 후보는 무료로 취급하거나 예산 충족을 보장하지 않는다. 분석 시연은 점수에 개입하지 않는다. 팀 검토안·문의 후보 별도 지정 단계는 없다.

## 문의와 협업·성과

```mermaid
flowchart TD
    A["카드·상세·비교표에서 한 명 문의"] --> B["해당 후보 편집기·기존 기록 복원"]
    B --> C["문안 확인·선택 AI 다듬기·제작 조건"]
    C --> D{"문의 방식"}
    D -- 발송 시연 --> E["문안·시각을 새 이력으로 저장"]
    D -- 복사·메일 초안 --> F["사용자가 외부에서 발송"]
    F --> FR["실제 날짜 입력·외부 문의 기록 버튼"]
    FR --> E
    E --> SH["보낸 문의 탭 자동 열기·문안 전체 표시"]
    SH --> NAV["보낸 문의 메뉴·협업 목록에서 재열기"]
    NAV --> SH
    SH -- 추가 문의 작성 --> C
    SH -- 답변 받았어요 --> H["조건 협의: 받은 견적·합의 비용·제작 조건"]
    H -- 협업 확정 --> I["제작 진행: 일정·조건"]
    I -- 게시 완료 --> J["집행 완료: 콘텐츠·성과·확인일·출처"]
    B --> S["현재 진행 상태 직접 선택"]
    S --> M["누락 안내·기존 조건과 성과 보존"]
    M --> V{"입력 값 유효?"}
    J --> UR["주소 접두사 보완·조건부 필수 표시"]
    UR --> V
    V -- 아니오 --> R["입력 유지·오류 탭/펼침 열기·필드 초점"]
    R --> B
    V -- 예 --> W["후보별 저장"]
    W --> CLOSE["편집창 닫기: 같은 리포트에서 재저장해도 적용"]
    CLOSE --> O["성과 리포트: 알려진 값·비용효율"]
    O --> P["선택 AI 요약·최대8명"]
    O --> Q["예시 리포트: 실제 기록과 분리"]
    B -- 닫기 --> U{"미저장 변경?"}
    U -- 있음 --> Z["계속 작성 / 저장 후 닫기 / 저장 없이 닫기"]
    U -- 없음 --> L["원래 목록"]
```

첫 문의 시연은 답변 대기로 바꾸고 실제 문의 날짜를 만들지 않는다. 추가 문의는 기존 협업 상태를 유지한다. 날짜를 편집하거나 진행 상태만 바꾸는 행위는 새 발송이 아니다. 시연·외부 직접 기록을 구분하며 이전 문안은 재발송 후에도 보존한다. 메뉴 숫자는 발송 건수다. 실제 외부 발송·배달 확인은 구현하지 않았다.

목록의 상태 선택은 즉시 저장하고 편집기의 상태 선택은 저장 후 반영한다. 자료가 아직 없어도 현실의 진행 상태를 기록할 수 있지만, 잘못 입력한 값과 성과 출처 누락은 저장을 막는다. 성과는 CSV와 추천 점수에 합치지 않는다.

## 캠페인 분석·근거·문의 연결

```mermaid
flowchart TD
    A["작은 적합 분석 버튼"] --> B["캠페인·CSV·연결 콘텐츠의 분석 결과 구성"]
    B --> C{"같은 자료의 캐시 있음?"}
    C -- 예 --> S["최종 문단·근거·제안·선택 질문"]
    C -- 아니오 --> K{"선택적 모델 설정?"}
    K -- 없음 --> L["로컬 결과 사용"]
    K -- 있음 --> R["제공 근거로 요청·실제 대기 표시"]
    R --> V{"응답·근거 ID 유효?"}
    V -- 아니오 --> E["오류: 다시 분석 / 확인된 자료 보기"]
    E -- 재시도 --> R
    E -- 자료 보기 --> L
    V -- 예 --> S
    L --> S
    S -- 근거 클릭 --> P["같은 창의 게시물·광고 댓글·원지표"]
    P -- 돌아가기 --> S
    P -- 자료 추가 --> N["필수값·숫자·URL·댓글 검증"]
    N -- 오류 --> NE["입력 유지·보완"]
    NE --> N
    N -- 유효 --> U["별도 자료 저장·이전 분석 무효화"]
    U --> B
    S -- 내려받기 또는 복사 --> X["화면과 같은 문단·근거의 리포트"]
    S -- 질문 선택·제안 담기 --> W{"기존 문의 있음?"}
    W -- 없음 --> NW["선택한 질문과 제작 제안으로 신규 초안"]
    W -- 있음 --> PV["추가할 문안 미리보기"]
    PV -- 반영 --> AP["기존 문안 뒤에 추가·기존 단계 보존"]
    PV -- 추가하지 않음 --> OLD["기존 문의 그대로 열기"]
    PV -- 닫기 --> CANCEL["기존 기록 변경 없음"]
    NW --> EDIT["한 명의 문의 편집기"]
    AP --> EDIT
    OLD --> EDIT
```

추천 순서는 별도 순수 함수가 결정하며 콘텐츠 해석으로 점수를 바꾸지 않는다. 5명 추가 예시는 실제 SNS 수집이 아니다. 자료 없는 후보는 CSV 근거만 읽는다. 실제 타깃 도달은 콘텐츠 표현과 별도로 확인한다.

닫기·자료 갱신 시 진행 중 분석 요청을 취소한다. 키가 없는 현재 환경에서는 로컬 결과를 사용한다. 모델 응답 검증은 형식·근거 ID 확인이며 의미상 정확성 검증과 다르다. 현재 실제 모델 응답은 미검증이다. 분석의 고정 2.1초 지연은 제거했다.

일반 주소는 캠페인별 localStorage, `?demo`는 별도 sessionStorage를 사용한다. 체험 탭의 새로고침도 기록을 유지하며 일반 캠페인 기록과 섞지 않는다. 문의/성과의 기존 AI 초안은 로컬 규칙으로 구성하고 사용자가 편집·적용한다.
