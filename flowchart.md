# 추천 흐름

정책: [PRD](PRD.md). 계산: `src/domain.ts`. 화면: `src/App.tsx`.

```mermaid
flowchart TD
    A["앱 열기"] --> B["원본 CSV 불러오기"]
    B --> C{"로딩·구조·값 검증 성공?"}
    C -- 아니오 --> D["오류와 다시 불러오기"]
    D --> B
    C -- 예 --> E["예산·카테고리 OR·규모 입력"]
    E --> F{"적용할 입력 유효?"}
    F -- 아니오 --> G["필드 오류 표시 / 적용된 결과 유지"]
    G --> E
    F -- 예 --> H["카테고리·규모 필터"]
    H --> I{"이력과 양수 평균 예산 있음?"}
    I -- 아니오 --> J["견적 확인 필요 / 조회수순 / 점수 없음"]
    I -- 예 --> K{"과거 평균 예산이 상한 이하?"}
    K -- 아니오 --> L["예산 초과 수 집계"]
    K -- 예 --> M["고정 플랫폼·규모 비교 집단"]
    M --> N{"집단 10명 이상?"}
    N -- 아니오 --> O["플랫폼 전체 → 전체 데이터 순으로 확대"]
    N -- 예 --> P["참여율·조회수 상대 위치"]
    O --> P
    P --> Q{"평점 있음?"}
    Q -- 아니오 --> R["미평가 표시 / 계산에만 중립 50"]
    Q -- 예 --> S["1~5점을 0~100으로 환산"]
    R --> T["집행 경험 체감 가산"]
    S --> T
    T --> U["가중합과 동일 객체에서 설명 생성"]
    U --> V{"적격 후보 있음?"}
    L --> V
    J --> V
    V -- 예 --> W["후보 카드 / 적용 조건 / 원지표 / 점수"]
    V -- 아니오 --> X["원인: 조합 없음·예산 초과·견적 미확인"]
    X --> Y["가능한 대안: 최소 예산·다른 규모 / 예상 인원"]
    Y --> Z{"사용자가 변경 적용?"}
    Z -- 예 --> E
    Z -- 아니오 --> X
    W --> AA{"정렬 변경?"}
    AA -- 예 --> AB["선택 지표 내림차순 / 미평가 마지막 / 동점 ID순"]
    AB --> W
    AA -- 아니오 --> AC["기여도·집단·표본 수 확인"]
    W --> E
```

필터 변경은 적용 버튼 이후 반영된다. 대안은 사용자 클릭으로만 적용한다. 견적 미확인 후보는 적격 추천과 별도로 항상 확인할 수 있다. 점수 집단은 적용 후보가 아닌 전체 원본 기준이다.

입력/로딩/결측 등 예외 정책의 실제 검증 상태는 [QA](docs/QA.md)에 기록한다.

