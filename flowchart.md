# 사용자 여정과 추천 흐름

정책은 [PRD](PRD.md), 사용자 피드백과 정보 구조는 [UX 검토](docs/UX_REVIEW.md), 계산은 `src/domain.ts`, 화면은 `src/App.tsx`에 연결된다.

```mermaid
flowchart TD
    A["데모 진입 / 서비스에서는 가입 완료 직후"] --> B{"유효한 로컬 조건 있음?"}
    B -- 아니오 --> C["캠페인명·제품 메시지·목표 / 공란 검증"]
    C --> BA["직접 입력 또는 AI 시연 문장 정리 / 조건 초안 수정"]
    BA --> D["분야·예산·팔로워 조건"]
    B -- 예 --> E["저장 조건으로 탐색"]
    D --> F{"입력 유효?"}
    F -- 아니오 --> G["필드 오류 / 수정"]
    G --> D
    F -- 예 --> WT["프리셋·직접 비중 기본 공개 / 합계100"]
    WT --> TR["온보딩 전환 1.6초 / 표현 시간"]
    TR --> E
    E --> H["CSV 로딩·구조·값 검증"]
    H --> I{"데이터 정상?"}
    I -- 아니오 --> J["오류 안내 / 다시 불러오기"]
    J --> H
    I -- 예 --> WL["Worker 요청 / 실제 처리 중 표시"]
    WL -- 계산 실패 --> WE["오류 안내 / 재시도"]
    WE --> WL
    WL --> K["분야 OR·팔로워 구간 필터"]
    K --> L{"이력과 양수 평균 비용 있음?"}
    L -- 아니오 --> M["비용 미확인 탭 / 조회수순 / 점수 없음"]
    L -- 예 --> N{"평균 비용이 예산 이하?"}
    N -- 아니오 --> O["예산 초과 제외"]
    N -- 예 --> P["고정 플랫폼·규모 집단"]
    P --> Q{"표본 10명 이상?"}
    Q -- 아니오 --> R["플랫폼 전체 → 전체 데이터로 확대"]
    Q -- 예 --> S["참여율·조회수 상대 위치"]
    R --> S
    S --> T{"평점 있음?"}
    T -- 아니오 --> U["미평가 / 계산 대체값 50"]
    T -- 예 --> V["평점 환산"]
    U --> W["경험 로그 환산 + 선택 기준의 가중합"]
    V --> W
    W --> X{"적격 후보 있음?"}
    O --> X
    M --> X
    X -- 아니오 --> Y["빈 원인과 대안 / 변경값·예상 인원"]
    Y --> Z{"사용자가 대안 적용?"}
    Z -- 예 --> E
    Z -- 아니오 --> Y
    X -- 예 --> AA["추천 목록 / 원지표와 주요 근거"]
    AA --> RE["캠페인 설정으로 재진입"]
    RE --> C
    RE -- 변경 없이 돌아가기 --> AA
    AA --> AB["프리셋 즉시 전환"]
    AB --> WL
    AA --> WC["직접 비중 조절 / 합계100 / 상위3 미리보기"]
    WC -- 취소 --> AA
    WC -- 적용 --> WL
    AA --> AI["재정렬 / 기존 점수 유지"]
    AI --> AA
    AA --> AC["상세: 검토 이유·예산·다음 확인·지표·예시 채널"]
    AC --> AD["후보 보관 / 3명 제한 없음"]
    M --> AD
    AA --> AD
    AD --> AJ["보관 목록에서 비교할 2~3명 선택"]
    AJ --> AE["현 조건·원지표·상대 강점과 한계 비교"]
    AE --> DS["검토 중 / 문의 후보 / 보류 지정"]
    AC --> DS
    DS --> HK{"문의 후보 1명 이상?"}
    HK -- 아니오 --> HS["문의 후보 지정 안내 / 검토로 복귀"]
    HS --> AD
    HK -- 예 --> AK["팀 검토안 / 추천 이유 초안 또는 직접 작성"]
    AK --> HR{"모든 문의 이유 작성?"}
    HR -- 아니오 --> AK
    HR -- 예 --> AF["문의 후보·이유·기준·한계 / 검토안 복사"]
    AF -- 복사 실패 --> HC["텍스트 선택 / 직접 복사"]
    AF --> AG["협업 관리 / 문의 준비"]
    DS --> AG
    HC --> AG
    AA --> AH["조건 변경 창"]
    AH --> CF{"변경 조건 유효?"}
    CF -- 아니오 --> AH
    CF -- 예 --> E
```

CSV 요청은 앱이 열릴 때 시작한다. 차트는 사용자가 마주하는 단계와 논리 분기를 표현한다. 기준 변경은 필터를 바꾸지 않고 점수·설명을 재계산한다. 재정렬만 바꾸면 점수는 유지되며 선택 지표 내림차순·동점 ID순이다. 조건 변경·대안 적용 후에는 추천순으로 돌아간다. 가격 미확인 후보에는 점수를 노출하지 않는다.

보관 목록·비교 선택·문의/보류 상태·메모·사용자 비중은 조건 변경과 재방문에도 보존한다. 후보를 빼면 비교 선택과 판단 상태에서도 제외한다. 문의 후보 한 명이면 상세에서 바로 검토안으로 이동한다. 네 번째 비교 선택은 기존 세 명을 유지하고 안내한다. 기준 안내 모달은 임시 선택 → 적용으로 동작하며 취소하면 기존 기준을 유지한다. 상단 빠른 전환은 즉시 적용한다.

가입·로그인·자동 문의 발송은 구현 범위 밖이다. 문의 초안과 수동 진행·성과 기록은 아래 흐름으로 제공한다. 모달은 닫기·Escape로 원래 화면에 돌아온다. 저장 오류가 나도 현재 탐색을 계속할 수 있다.


## 협업·성과 기록

```mermaid
flowchart TD
    A["문의 후보 지정 또는 팀 검토안"] --> B["협업 관리 / 문의 준비"]
    B --> C["직접 확인한 연락처·제작 범위·일정·사용권"]
    C --> BA["직접 입력 또는 AI 시연 문장 정리 / 조건 초안 수정"]
    BA --> D["문의 기본 문안 수정 / 선택적 AI 다듬기"]
    D --> E["복사 또는 이메일 앱의 초안 열기"]
    E --> F["사용자가 외부에서 실제 문의"]
    F --> G["문의 날짜·답변 대기 또는 조건 협의 기록"]
    G --> H{"문의 날짜·합의 비용·제작 범위 있음?"}
    H -- 아니오 --> I["누락 안내 / 수정"]
    I --> C
    H -- 예 --> J["제작 진행 / 수동 상태 저장"]
    J --> K["콘텐츠 링크·실제 비용·확인일·출처·성과 입력"]
    K --> L{"숫자·날짜·출처·구매 집계 기준 유효?"}
    L -- 아니오 --> K
    L -- 예 --> M["크리에이터별 최신 누적 성과 저장"]
    M --> N["원지표·미입력·계산 가능한 비용 효율"]
    N --> O["성과 리포트 / 지표별 입력 인원 / 상세"]
    O --> K
    O --> P["선택적 AI 요약 / 최대8명 / 사용자 검토"]
    O --> Q["예시 리포트 / 합성 자료 명시 / 저장 안 함"]
    Q --> O
```

복사·메일 초안 열기는 연락 상태를 바꾸지 않는다. 제공 데이터에는 실제 연락처가 없다. 실제 발송과 수치는 자동 수집하지 않는다. 성과는 CSV·추천 점수에 합치지 않으며, 단일 콘텐츠의 최신 누적 기록이다.

## AI 시연의 사용자 흐름

```mermaid
flowchart LR
    A["AI 시연 진입 / 명시한 예시 모드"] --> B["브리프 문장 또는 기존 문안·성과 기록"]
    B --> C["사용자가 초안 만들기 / 표현 대기 0.9초"]
    C -- 종료·자료 변경 --> X["예정 결과 취소 / 기존 값 유지"]
    C --> D["로컬 규칙으로 초안 / 제공 정보만 사용"]
    D --> E["편집 가능한 결과 확인"]
    E -- 다시 입력 --> B
    E -- 브리프 적용 --> F["검토한 제품·조건·우선순위 / 비중 설정으로 이동"]
    E -- 문의 문안 사용 --> G["보낼 문안에 반영 / 자동 발송 안 함"]
    E -- 성과 요약 복사 --> H["팀 문서에 붙여 넣을 요약"]
```

기본 화면은 실제 모델을 호출하지 않는다. 브리프 시연은 사용자가 검토한 조건을 명시적으로 적용하며 이후 순위는 추천 함수가 계산한다. 내부 선정 이유는 팀 검토안에 보존하고 외부 문의 문안에 자동 포함하지 않는다. 실제 API 연결 코드는 별도 개발용이며 시연과 구분한다. `?demo`는 저장하지 않는 독립 체험이다. 결제·팀 권한·복수 캠페인은 향후 범위다.
