# Creator Match · 사용자 흐름과 추천 처리

[PRD](PRD.md)의 화면 ID를 사용합니다. **① 어디로 이동하는지 → ② 입력을 어떻게 확정하는지 → ③ 언제 계산하는지 → ④ 어떻게 추천하는지 → ⑤ 결과가 없으면 무엇을 하는지 → ⑥ 협업을 어떻게 기록하는지**를 나누었습니다.

## 1. 전체 화면 흐름

실선은 이동 가능한 경로입니다. 상세·비교·분석을 모두 거쳐야 문의할 수 있다는 의미가 아닙니다.

```mermaid
flowchart LR
    S00["S00 캠페인 설명"] --> S01["S01 캠페인·고객"]
    MANUAL["직접 입력"] --> S01
    S01 --> S02["S02 후보 조건"]
    S02 --> S03["S03 추천 기준"]
    S03 --> S04["S04 추천 목록"]
    S04 --> S05["S05 기준 변경"]
    S05 -->|적용 또는 취소| S04
    S04 --> S06["S06 상세·적합 분석"]
    S04 --> S07["S07 저장·비교"]
    S04 --> S08["S08 문의 준비"]
    S06 --> S08
    S07 --> S08
    S08 --> S09["S09 협업 관리"]
    S09 --> S10["S10 성과 입력·리포트"]
    S04 -->|조건 편집| S02
```

S06·S07은 닫기 또는 찾기 메뉴로 목록에 돌아옵니다. 왼쪽 메뉴에서는 저장·보낸 문의·협업·리포트로 직접 이동할 수 있습니다. S10은 집행 완료 단계에서만 열리는 기능이 아닙니다.

## 2. 입력 확정과 보존

아래는 새 캠페인의 시작 경로입니다. 기존 캠페인 편집은 현재 값을 초안으로 복사하고 취소 시 적용값을 유지합니다.

```mermaid
flowchart TD
    A{"설명 입력 방식"} -->|문장으로| B["명시한 값만 추출"]
    A -->|직접 입력| C["S01 캠페인·고객"]
    B --> C
    C --> D{"이름·제품 설명 유효?"}
    D -->|아니오| E["오류 필드 초점 · 나머지 입력 유지"]
    E --> C
    D -->|예| F["S02 예산·분야·규모"]
    F --> G{"양의 정수 예산 · 분야 1개 이상 · 규모 선택?"}
    G -->|아니오| H["필드별 오류 · 입력 유지"]
    H --> F
    G -->|예| I["S03 프리셋 또는 직접 비중"]
    I --> J{"비중 유효 · 합계 100%?"}
    J -->|아니오| I
    J -->|예| K["조건 확정 · 캠페인 저장"]
    K --> L["추천 안내 전환 · S04"]
    F -->|이전| C
    I -->|이전| F
    C -.->|기존 편집 취소| X["기존 조건·결과 유지"]
    F -.->|기존 편집 취소| X
    I -.->|기존 편집 취소| X
```

추출은 현재 로컬 규칙입니다. 입력하지 않은 필수 조건을 기본값으로 확정하지 않으며 수동 이름·비중은 재정리보다 우선합니다. 이전 이동의 입력 보존과 새로고침 복구는 다릅니다. 미확정 초안은 새로고침으로 유실될 수 있습니다.

## 3. 데이터 준비와 추천 계산

CSV 로딩은 온보딩과 동시에 시작됩니다. 4.2초의 안내 전환을 실제 서버 진행률로 표현하지 않습니다.

```mermaid
sequenceDiagram
    actor U as 사용자
    participant UI as 화면
    participant CSV as CSV 로더
    participant W as 추천 Worker
    UI->>CSV: 앱 시작 시 요청
    par 데이터 준비
        CSV-->>UI: 파싱·검증 결과 또는 오류
    and 조건 준비
        U->>UI: S01~S03 확인·적용
        UI->>UI: 조건 저장·안내 전환 시작
    end
    alt 데이터 요청·검증 실패
        UI-->>U: 오류와 다시 불러오기
        U->>UI: 재시도
        UI->>CSV: 재요청
    else 데이터와 확정 조건 준비
        UI->>W: 원본 객체·조건·비중
        alt 계산 성공
            W-->>UI: matched·needsQuote·alternatives·summary
            UI-->>U: 안내 전환 종료 후 S04 표시
        else Worker 시작·실행 실패
            UI-->>U: 계산 오류와 재시도
        end
    end
    opt 조건 또는 비중 변경
        UI->>W: 이전 Worker 정리·새 작업 요청
        W-->>UI: 새 입력에 대응하는 결과
        UI-->>U: 현재 요청의 결과만 표시
    end
```

오류 상태는 다시 시도해 복구하며, 로딩 중 이전 결과를 새 조건의 결과로 보여주지 않습니다. 모델 호출은 이 추천 계산에 포함되지 않습니다.

## 4. 후보 판정과 점수 계산

첫 그림은 **후보 집합을 분리하는 과정**, 두 번째는 **적격 후보 한 명의 점수 계산**입니다. 분기된 후보를 한 명씩 ‘결과 없음’으로 판정하지 않고 전체 집합을 만든 뒤 결과 화면이 판단합니다.

```mermaid
flowchart TD
    A["검증된 200명 원본 객체"] --> B["분야 OR · 규모 필터"]
    B --> C["조건에 맞는 집합"]
    C --> D["건수 0 또는 평균 비용 0인 집합"]
    C --> E["건수와 평균 비용이 양수인 집합"]
    D --> Q["needsQuote · 평균 조회수순 · 동점 ID순"]
    E --> F["평균 비용이 예산 이하"]
    E --> G["평균 비용이 예산 초과"]
    F --> H["각 후보 점수 계산"]
    H --> M["matched · 총점순 · 동점 ID순"]
    G --> N["초과 인원·최소 참고 예산"]
    B --> S["단계별 제외 요약"]
    N --> T["대안 조건과 적용 후 인원 계산"]
    Q --> R["MatchResult 반환"]
    M --> R
    S --> R
    T --> R
```

```mermaid
flowchart TD
    A["적격 후보 1명 + 필터 전 전체 원본"] --> B["동일 플랫폼×규모 집단"]
    B --> C{"10명 이상?"}
    C -->|아니오| D["동일 플랫폼 전체"]
    D --> E{"10명 이상?"}
    E -->|아니오| F["원본 전체"]
    E -->|예| G["집단 확정"]
    C -->|예| G
    F --> G
    G --> H["참여율 E · 조회수 V 백분위"]
    A --> I{"평점 존재?"}
    I -->|예| J["R = 평점 척도 환산"]
    I -->|아니오| K["R 계산값 50 · 표시는 미평가"]
    A --> L["H = 경험 로그 환산"]
    H --> M["E·V·R·H에 적용 비중을 곱해 합산"]
    J --> M
    K --> M
    L --> M
    M --> N["총점·항목별 기여·비교 집단 반환"]
    N --> O["같은 결과로 순위와 상세 설명 생성"]
```

현재 평점 공란 27명은 모두 needsQuote로 분리되므로 점수 그림의 공란 분기를 통과하지 않습니다. 향후 유료·미평가 후보까지 처리하는 방어 분기입니다. 비중·수식·단일 집단·동점 정책은 [PRD §6](PRD.md#6-추천-로직과-설계-근거)을 따릅니다.

## 5. 목록·검색·빈 결과 복구

플랫폼·키워드는 추천 이후의 표시 범위를 좁힙니다. 비중은 점수를, 예산·분야·규모는 후보 자격을 바꿉니다.

```mermaid
flowchart TD
    A["matched와 needsQuote"] --> B["플랫폼·선택 검색 조건 적용"]
    B --> C{"보여줄 후보가 있는가?"}
    C -->|예| D["S04 목록 · 원지표 · 다음 행동"]
    C -->|아니오| E{"현재 범위에서 원인 확인"}
    E --> F["플랫폼 제한: 전체 채널 제안"]
    E --> G["키워드 제한: 해제 또는 자료 필요 그룹"]
    E --> H["비용 미확인만 있음: 견적 필요 탭"]
    E --> I["예산 초과: 최소 비용·차액·인원"]
    E --> J["분야·규모 없음: 가능한 규모·인원"]
    I --> K{"제안을 적용했는가?"}
    J --> K
    K -->|예| L["조건 저장 · 후보 자격 다시 판정"]
    K -->|아니오| E
    L --> A
    F --> B
    G --> B
    H --> D
    D -->|원지표 정렬| M["원값으로 재정렬 · 점수 유지"]
    M --> D
    D -->|비중 적용| N["같은 적격 집합의 점수 재계산"]
    N --> A
```

자료가 없는 경우와 등록 자료에서 검색어가 확인되지 않은 경우를 구분합니다. 제외어 일치가 우선하고 포함어는 OR/AND에 따라 판정합니다. 원본 추천 점수에는 검색어·자료 보유 가산이 없습니다. 제안의 인원도 현재 검색 범위를 반영합니다.

## 6. 분석에서 문의·성과로 이어지는 선택 경로

분석을 생략하고 바로 문의할 수 있습니다. 분석 결과·근거·선택 질문은 하나의 검토 객체를 공유합니다.

```mermaid
flowchart TD
    A["목록·상세·비교"] -->|바로 문의| I["S08 문의 초안 열기"]
    A -->|분석 요청| B["S06 모달·로딩"]
    B --> C{"모델 연결 설정?"}
    C -->|없음| D["로컬 근거 분석"]
    C -->|있음| E["모델 요청·근거 ID·응답 형식 검사"]
    E -->|실패| F["오류: 재시도 또는 로컬 분석 선택"]
    F -->|재시도| E
    F -->|로컬 선택| D
    E -->|통과| G["문단·근거·제작 제안·질문"]
    D --> G
    G --> H["선택 질문 확인 · 기존 문안이면 추가 미리보기"]
    H --> I
    I --> J["문안·조건 저장 · 기존 상태 보존"]
    J --> K["복사·메일앱 초안 또는 로컬 문의 이력"]
    J --> L["S09 견적·일정·사용권·상태 기록"]
    L --> M["S10 성과 입력"]
    M --> N{"수치·출처·날짜·집계 기준 유효?"}
    N -->|아니오| O["필드 오류·입력 보존"]
    O --> M
    N -->|예| P["저장·모달 닫기·리포트 갱신"]
```

협업 상태는 아래와 같은 일반 순서로 정리했지만 **현재 구현은 수동 자유 이동**입니다. 단계 누락이 전이를 막지 않고, 상태를 되돌려도 기록을 삭제하지 않습니다.

| 현재 상태 | 대표 다음 상태 | 부작용·보존 |
|---|---|---|
| 문의 준비 | 답변 대기 | 로컬 문의 기록 시 초안 상태만 이동, 실제 연락일 생성 없음 |
| 답변 대기 | 조건 조율 | 받은 견적·문안 보존 |
| 조건 조율 | 제작 진행 | 합의 비용·일정·사용권 보존 |
| 제작 진행 | 집행 완료 | 수동 상태 변경, 성과 필수 제출 없음 |
| 모든 상태 | 진행 안 함 또는 다른 상태 | 기존 기록 보존, 종료 사유 입력 가능 |

성과는 수동 관측값이며 추천 점수로 환류하지 않습니다. 빈 값은 미확인, 0은 관측된 0입니다. 저장 성공과 외부 발송 성공도 서로 다른 상태입니다.
