# 입력부터 추천까지의 흐름

현행 [PRD](PRD.md)와 코드 기준입니다. 첫 번째와 두 번째 그림이 핵심 추천이며, 세 번째는 선택 기능입니다.

## 1. 캠페인 입력과 검증

```mermaid
flowchart TD
    A["새 캠페인 또는 설정 변경"] --> B{"설명으로 입력할까?"}
    B -->|예| C["문장에서 명시된 값 추출 · 로컬 규칙"]
    B -->|아니오| D["직접 입력"]
    C --> E["1단계 · 캠페인과 고객 확인"]
    D --> E
    E --> F{"이름과 제품 설명이 유효한가?"}
    F -->|아니오| G["필드 오류 · 초점 이동 · 입력 보존"]
    G --> E
    F -->|예| H["2단계 · 예산과 복수 분야와 규모"]
    H --> I{"양의 정수 예산 · 분야 1개 이상 · 규모 선택?"}
    I -->|아니오| J["누락 또는 잘못된 입력 표시"]
    J --> H
    I -->|예| K["3단계 · 목표 기반 시작 비중 확인"]
    K --> L["프리셋 선택 또는 직접 비중 조절"]
    L --> M{"각 비중이 유효하고 합계가 100%인가?"}
    M -->|아니오| L
    M -->|예| N["조건 확정 · 안내 전환"]
    N --> O["데이터 로딩과 추천 실행"]
    H -->|이전| E
    K -->|이전| H
    E -->|기존 설정 취소| P["기존 캠페인 결과 유지"]
```

명시하지 않은 예산·분야·규모는 임의 확정하지 않습니다. 직접 설정한 이름·비중은 재정리보다 우선합니다. 세 단계 모두 같은 좌우 배치를 사용합니다.

## 2. 데이터 처리·점수·결과 복구

```mermaid
flowchart TD
    A["CSV 불러오기"] --> B{"요청과 데이터 검증 성공?"}
    B -->|아니오| C["오류 안내 · 다시 불러오기"]
    C --> A
    B -->|예| D["원본 보존 · 숫자와 평점 null로 변환"]
    D --> E["선택 분야 OR · 선택 규모 필터"]
    E --> F{"집행 이력과 평균 비용이 있는가?"}
    F -->|아니오| G["비용 미확인 후보 · 평균 조회수순"]
    F -->|예| H{"과거 평균 비용이 예산 이하인가?"}
    H -->|아니오| I["예산 초과 집계"]
    H -->|예| J["원본의 동일 플랫폼과 규모 집단 선택"]
    J --> K{"집단이 10명 이상인가?"}
    K -->|아니오| L["동일 플랫폼 전체 · 부족하면 전체 데이터"]
    K -->|예| M["참여율과 조회수 백분위"]
    L --> M
    M --> N{"평점이 있는가?"}
    N -->|예| O["평점 1~5 척도 환산"]
    N -->|아니오| P["계산 대체값 50 · 표시는 미평가"]
    O --> Q["경험 로그 변환 · 사용자 비중 가중합"]
    P --> Q
    Q --> R["총점 내림차순 · 동점은 ID"]
    R --> S{"적격 후보가 있는가?"}
    I --> S
    E -->|분야와 규모에 후보 없음| S
    S -->|예| T["추천 목록 · 원지표 · 상세 설명"]
    S -->|아니오| U["빈 결과 원인 · 가능한 조건 완화와 실제 인원"]
    G --> S
    G --> V["별도 후보 목록 · 견적 문의 경로"]
    U --> V
    U --> W{"조건 변경을 적용했는가?"}
    W -->|예| E
    W -->|아니오| U
    T --> X{"다음 행동"}
    X -->|원지표 재정렬| Y["참여율 · 조회수 · 집행 · 평점 정렬"]
    Y --> T
    X -->|비중 적용| J
    X -->|조건 변경| E
    X -->|상세 확인| Z["실제 비중과 비교 집단 · 후보 특징"]
```

점수 기준 집단은 필터 전 원본으로 고정합니다. 공란 평점의 계산 분기는 향후 데이터까지 처리하지만, 현재 원본의 공란 27명은 모두 비용 미확인 분기로 이동합니다. 최소 예산·다른 규모를 제안할 때에도 사용자의 적용 없이 조건을 바꾸지 않습니다.

## 3. 선택 경로: 콘텐츠 근거부터 문의·성과까지

```mermaid
flowchart TD
    A["추천 또는 비용 미확인 후보"] --> B["추가 검색 · 포함 또는 제외 조건"]
    B --> C{"콘텐츠 자료와 일치 근거가 있는가?"}
    C -->|확인| D["확인된 후보"]
    C -->|자료 없음| E["자료 필요 후보"]
    C -->|등록 범위 내 불일치 또는 제외어| F["제외 요약 · 조건 편집"]
    D --> G["상세 · 저장 · 최대 3명 비교"]
    E --> G
    G --> H["적합 분석 요청 · 로딩"]
    H --> I{"선택적 모델 연결 사용?"}
    I -->|아니오| J["로컬 근거 분석"]
    I -->|예| K["캠페인과 근거 전달 · 응답 검사"]
    K -->|연결 또는 형식 오류| ERR["오류 안내"]
    ERR -->|다시 분석| K
    ERR -->|확인된 자료로 보기 선택| J
    K -->|유효 응답| L["문단 · 근거 · 제작 제안 · 질문"]
    J --> L
    L --> M["콘텐츠나 댓글 근거 확인"]
    M --> L
    L --> N["선택 질문을 문의에 추가 · 기존 문안 미리보기"]
    N --> O["문의 문안과 협업 상태 저장"]
    O --> P["문안 복사 또는 이메일 초안 열기"]
    O --> Q["견적 · 제작 · 집행 기록"]
    Q --> R["성과 수치 입력"]
    R --> S{"수치 · 출처 · 확인일 · 구매 집계 기준 유효?"}
    S -->|아니오| T["오류 표시 · 입력 보존"]
    T --> R
    S -->|예| U["저장 · 입력 창 닫기 · 성과 리포트"]
```

문의 기록은 로컬 상태이며 외부 발송이 아닙니다. 성과는 수동 관측 기록이고 추천 점수로 환류하지 않습니다. 키워드·콘텐츠 자료 보유 여부도 원본 추천 점수에 가산하지 않습니다.
